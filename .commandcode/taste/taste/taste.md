# Taste
- Content sections/tabs that can legitimately be empty (e.g., profile tabs 笔记/评论/收藏/赞过) must render a centered gray "暂无X" placeholder (暂无笔记/暂无评论/暂无收藏/暂无赞过) when there is no data — an empty tab area that shows nothing is not acceptable, and the same empty-state text applies when viewing other users' profiles as on one's own. Confidence: 0.6
- Privacy semantics for profile content visibility (笔记/评论/收藏/赞过): switches default to ON, and a section switched OFF is completely invisible to other viewers — its tab text and its content both disappear entirely (no lock icon or "hidden content" hint shown to them); the owner's own view keeps the section but marks it with a lock icon. Lock icons must never appear for content that is actually public — showing a lock unconditionally is treated as wrong. Confidence: 0.55
- Communicates in Chinese; prefers responses and discussion in Chinese. Confidence: 0.8
- Prefers to replicate the structure and layout logic of an existing screen when creating a new similar page, to keep the UI consistent across related screens — explicitly endorses copying existing code directly (e.g., "代码可以直接照搬") when a new page mirrors an existing one. Confidence: 0.95
- Prefers using the project's theme color helper functions (e.g., getOutline, getOnSurfaceSecondary, getOnSurfaceTertiary) rather than hardcoded colors; UI specs name colors as theme tokens with explicit alphas (e.g., "颜色为surface透明度80%", "颜色为getOnSurfaceSecondary()") and expect those exact tokens used. Confidence: 0.95
- When specifying a UI screen, gives precise pixel-level specs (exact dp spacing, corner radii, max-width ratios, color tokens) and expects the implementation to match them exactly. Confidence: 0.95
- Prefers compact controls that hug their content: input bars should be roughly text height plus small padding (e.g., 5dp vertical), not the taller default Material paddings, and sibling controls (e.g., a send button next to an input field) should be equal height. Confidence: 0.6
- Expects standard keyboard UX on input/chat screens: the IME keyboard pushes the content up (imePadding) and tapping blank space dismisses the keyboard. Confidence: 0.7
- For pages backed by cloud data, expects a ViewModel + ViewModelFactory wired through navigation, with optimistic UI updates that sync to the backend. Confidence: 0.6
- Prefers unread-count badges to be circular, with the theme primary color as background and surface color for the digits, anchored at the top-right corner of the icon (and the same badge style on the bottom "消息" nav tab when a private message arrives), matching WeChat/Xiaohongshu conventions. Confidence: 0.8
- Prefers social interactions (likes, favorites, follows, comments, private messages) to be real-time and backed by the backend rather than locally simulated/mock data. Confidence: 0.85
- Validates real-time/social features end-to-end with a second account: performs the action from another account, switches accounts, and expects the notification list, unread badges, and chat to reflect it immediately. Confidence: 0.7
- When a feature "doesn't work" (e.g., a feed shows empty though the user knows data was published), expects the assistant to verify the actual live backend state and data source (e.g., which Supabase table holds the rows, whether the query targets the right table) and find the root cause, not just confirm local builds pass. Confidence: 0.65
- For search results that group by category, wants a Xiaohongshu-style loose match: any roughly-matching item (按大致符合/模糊) is shown, with a category header row (small tertiary-colored, left-aligned, small vertical padding) above each group's list; when a result row is tapped, the matched keyword segments inside its text (names/messages, case-insensitive, every occurrence) are highlighted in the theme primary color via AnnotatedString. Confidence: 0.5
- For dedicated search pages reached by tapping a search icon, expects an explicit screen-level layout contract: horizontal ~10dp page padding, the search input row positioned ~24dp from the top, a search icon + placeholder text (both in onSurfaceSecondary) inside a rounded (~15dp) background-colored input field, a "取消" (cancel) text button ~5dp to the right that returns to the previous screen, primary-colored input cursor, and a clear (close-ring) icon at the field's end only while there is input, which empties the field. Confidence: 0.7
- Full-screen content pages (e.g., Douyin-style video feed) must always show a top bar with a back button — including in loading and empty ("暂无…") states — so the user can always exit; an empty state with no way to navigate back is a bug. Such pages must also be pushed onto the navigation back stack (navigateTo) when opened from a tab/main screen — replacing the entire stack turns the page into a root with nothing to go back to, so the back button/goBack silently does nothing and the user reports it as "no back icon". Confidence: 0.8
- Prefers buttons whose label can change (e.g., 关注 → 已关注 → 发私信) to keep a fixed width with centered content and constant internal padding, so changing the text length never changes the button width or shifts the layout. Confidence: 0.85
- On user-list rows (e.g., followers), the follow state must be reflected correctly and be toggleable: an un-followed user must show a "关注" button (not "发私信" — showing 发私信 for someone you don't follow is a bug), and the row keeps a single fixed-width button whose label switches between 关注 and 发私信 based on follow state — do NOT add extra icons next to the follow button (the user explicitly rejected such an icon). Follow actions must toggle the current state (!followed) rather than hardcode to "follow". Confidence: 0.8
- Expects account-to-account messaging bridges wherever another user is surfaced (follower list rows, post-detail author bar): tapping "发私信" directly creates/opens the conversation with that user and lands on the corresponding ChatScreen. Confidence: 0.6
- In chat screens, prefers avatars to show on both sides: own sent messages display the current user's avatar on the right and peer messages display the peer's avatar on the left — sent messages should not appear avatar-less. Confidence: 0.7
- Expects account switching to be clean and leak-free: session-scoped chat state (peerUid, conversationId) must reset on logout, account-scoped ViewModels/lists (conversations, notifications) must reload for the new account, and app-level singleton caches of account-derived data (e.g., the resolved IP/location province used for profile and post-detail fallbacks) must be cleared on logout/switch and re-resolved for the new account — seeing the previous account's data or reusing its cached values after switching accounts is a bug. Confidence: 0.6
- Cares about page load/rendering performance (e.g., the profile "我的" page notes area loading slowly after switching accounts) and expects it flagged and addressed — but only where a real win is possible: when the current approach is already the reasonable optimum (e.g., concurrent async queries), the assistant should explain the bottleneck rather than make speculative changes, and the user is open to hearing "leave it as-is unless you want lazy-load/caching". Confidence: 0.6
- In the conversation list row, prefers the unread-count badge at the end of the second row (right of the last-message preview, aligned to the row end, WeChat style) — explicitly NOT overlaid on the avatar's top-right corner — and keeps it small/compact (e.g., ~18dp circle, ~9sp digits). Confidence: 0.8
- Messaging time display follows a fixed relative-time hierarchy: today shows HH:mm → 昨天 → 前天 → N天前 → N周前 → N月前 → older than a year shows yyyy-MM-dd; the same hierarchy should be applied consistently in both the conversation list and the chat screen. Confidence: 0.85
- In chat screens, mimics WeChat timestamp behavior: inserts a small centered time divider between adjacent messages when the gap exceeds ~5 minutes instead of stamping every message. Confidence: 0.7
- In chat, the sender's avatar must remain visible regardless of message length — long messages should wrap within a capped-width bubble rather than expanding the row and pushing the avatar off-screen. Confidence: 0.75
- When checking in on a multi-item request ("我说的任务完成了吗"), expects the assistant to track the requested items (e.g., a todo list) and report precisely which are done and which remain, then finish the remaining ones — not just claim "almost done". Confidence: 0.5
- For real user-generated content in lists (e.g., notification actor avatars, post cover thumbnails), expects the actual network data to be loaded and displayed (query the users table for the avatar, the post's image_url for the cover) rather than a hardcoded placeholder drawable; placeholders only as a fallback when loading fails. Confidence: 0.75
- In chat, prefers optimistic media sending: the sender's image/video bubble appears immediately using the local content URI and is swapped for the remote URL once upload completes, so the sender never sees their own sent media vanish or fail to render. Confidence: 0.65
- Send actions that allow attaching media must handle pure-media messages (empty text + images): do not gate sending on non-blank text, or image-only comments/messages get silently dropped. Confidence: 0.6
- Follow state must be re-synced from the backend when the page becomes visible again (ON_RESUME) rather than trusting locally-remembered state captured at first entry — unfollowing in one page must be reflected everywhere else. The user reiterates this as an explicit requirement that follow/关注状态 be synced to the cloud and stay consistent across the whole app ("关注状态要同步至云端和全局app"). Confidence: 0.85
- Profile page stats (粉丝/关注/获赞) must be computed from the backend (follows counts, sum of like_count on the user's own posts/videos) and refresh in real time — on real-time follow/like/comment notifications and on every page resume — never hardcoded placeholder zeros. Confidence: 0.8
- Third-party avatars/usernames across the app should be tappable and navigate to that user's profile page — comprehensively wired everywhere (post-detail author bar and content avatar, comment list incl. replies, video-detail author row and comments, chat top bar, followers list, reaction/comment notification rows), honoring explicit exclusions: card-bottom avatar/username, the user's own avatar in the comment input box, and conversation-list avatars do NOT navigate. Confidence: 0.85
- User-set per-user metadata (e.g., 备注名 remark) must be persisted to the cloud in a dedicated, relationship-independent table via upsert (not grafted onto a conditional follow row, which silently fails when the relationship doesn't exist), and the required Supabase SQL must be clearly communicated to the user for them to run. Confidence: 0.7
- Remarks (备注名) are private, viewer-scoped, cloud-stored aliases with a strong product semantic: once set, the remark becomes the user's global display name everywhere they appear to me (conversation list, chat title, notifications, follower list, other-user profile header, DM entry) — remark takes precedence over nickname (original nickname only shown as secondary text like "原昵称：xxx"); it is used persistently until explicitly changed/cleared, and can be set for anyone (not only followed users) in a dedicated viewer→target remarks table keyed independently of follow state. Confidence: 0.6
- In chat, pure-media messages (image/video with no text) should be rendered as the bare media itself — no chat-bubble background around it; the bubble styling applies only when the message also contains text. Confidence: 0.6
- Full-screen video players (video detail, Douyin-style feed pages) should pause/resume on a tap of the video area, showing a plain center pause icon (the video_fill drawable) with no surrounding translucent/dark circle background — the user found the black semi-transparent circle behind the icon odd and had it removed. Confidence: 0.7
- Follow-state buttons on other-user pages: clicking the button when already following (已关注/互相关注) must open a bottom action sheet (containing 取消关注 etc.) instead of directly unfollowing — direct state change only happens through an explicit action inside the sheet; only the not-following state (关注) clicks straight through to follow. Confidence: 0.7
- Action-sheet/menu rows that open a sub-page or sub-flow (e.g., 设置备注名) use a right-arrow chevron icon (arrow_right.xml), not an up/down expand icon. Confidence: 0.55
- Prefers white rounded-corner surfaces (setting/edit cards, info cards on the user-card 资料卡 page, bottom action sheets, dialogs) to have soft edge shadows for a more three-dimensional look — applied via Modifier.shadow matching the container's corner radius, with subtle low-alpha ambient/spot colors (e.g., ~4dp for 16dp cards, ~8dp for dialogs/sheets, ~2dp for small rows inside a sheet) rather than flat surfaces. Confidence: 0.8
- On the overlay/card surface of a full-screen page (e.g., the user-card 资料卡 page opened over another profile), gives relative positioning specs against the full screen and expects them honored as such: the card's vertical start position is given as a percentage of screen height ("资料卡区域再往上来10%" → start at 15%), and its width as a percentage of full screen width (90%), with page-level padding kept separate — implementing 90% against the already-padded content width instead of the whole screen reads as deviating from the spec. Confidence: 0.6
- Between rows inside an info card (e.g., ID/性别/生日/地区 rows on a user-card page), prefers thin (~0.5dp) horizontal divider lines in the theme outline color (getOutline) inserted between items — the user asked for them without an alpha qualifier, so plain getOutline is expected unless an opacity was explicitly specified. Confidence: 0.7
- When separating repeated items (e.g., the three notification columns on the messages page), prefers thin horizontal divider lines (~0.5dp) using the theme outline color at 50% opacity (getOutline().copy(alpha = 0.5f)); the divider must render as one straight, continuous line aligned with the row — wrapping each item in extra containers to place per-item dividers is rejected because it breaks the even distribution and makes the line crooked. Confidence: 0.65
- When the same screen serves both the current user's own data and another user's data (e.g., the profile page shown for self vs. for a viewed third party), expects strict isolation: keyed ViewModels (or equivalent) so the third-party view loads that user's own posts/comments/collections/likes, header background, and follow state — never leaking the viewer's own data (their posts, their follow status, their background) into the other-user page. Confidence: 0.75
- Account-scoped screens (own profile, post/video detail, followers, the home "关注" feed, likes/favorites state) must key their ViewModel by the current userUid and reload on account switch — never reuse a cached ViewModel whose userUid is stale from the previous account, which makes the page reflect the wrong account (e.g., logging in as user B still showing user A's followed posts, notes/comments, or comment author role). A fixed/constant key (e.g. key = "self_profile") is just as broken as no key, since it still reuses the old instance on switch — the key itself must embed userUid. And one-shot load effects should key off userUid (LaunchedEffect(userUid)) rather than LaunchedEffect(Unit) so they re-run on account change. Confidence: 0.8
- Content-detail pages generally share a consistent card-style presentation (light surface, ~3:4 media, 20sp bold title, 16sp body, a "time · IP · views" footer) with PostContent.kt as the canonical reference — but full-screen video pages are the exception: they should stay dark/immersive with the video filling the screen and the info (author/title/body/time·IP·views) overlaid as white text directly on the dark background, NOT on a light/opaque background block (a light background behind the info on a full-screen video reads as jarring/突兀). "Matching PostContent" means including the same info fields, not copying the light card background. Confidence: 0.7
- When asked to change a specific screen, do not modify unrelated files (e.g., moving a shared component's package or changing another screen's imports) unless explicitly requested — touching files the user didn't ask to change provokes an explicit demand to revert them. Scope also applies within a file: existing code not part of the request must stay untouched ("其他我没让你改的现有代码都不要动"), so edits should be strictly limited to the asked change. Confidence: 0.9
- When a change is applied to the wrong screen/location (e.g., a peer-profile menu button was added to the own-profile header instead), the user demands a verbatim revert ("把代码原封不动改回去"): the misapplied code must be restored to its exact prior state — every added parameter, callback, wiring, and icon swap removed — before the intended change is implemented only where it was meant to be; a partial revert leaving orphaned parameters or dead features is not acceptable. Confidence: 0.75
- In this app, "用户主页" in requirements means the profile page of the OTHER user being viewed (我看的用户的主页/对方主页) — never the logged-in account owner's own profile (自己/账号主的主页); conflating the two forces a full rework, so before implementing, disambiguate which screen a request targets when the app has both an own-profile page and a peer-profile page sharing similar headers. Confidence: 0.7
- On full-screen video pages using a native AndroidView/SurfaceView player, an always-visible top back bar must be drawn at the top z-order of the root Box (after/above the video layer), because a native video surface renders above Compose siblings and will cover a back bar that's just a normal column element — it flashes briefly then disappears once playback starts. Confidence: 0.65
- For full-screen video pages (post content and video detail alike), expects a fixed layering model: the parent container is full-screen with an onSurface (dark) background; the video sits at the bottom z-layer and fills the "video area" (the region above the progress bar); landscape videos fill the width with height adapting and vertical centering, portrait videos fill the width with the height centered and cropped to the video area (never stretched); avatar/title/back button and other info sit on a higher layer with transparent backgrounds so they never block the video, and every row/column background stays transparent. Confidence: 0.75
- On full-screen media pages, the bottom action bar is its own separate transparent region sitting 16dp from the bottom: left is a comment input box (16dp rounded corners, getOutline background, an edit icon + 5dp gap + "说点什么..." placeholder) and right is a like/favorite/comment row evenly distributed (SpaceEvenly) with unchanged logic; the progress bar sits above this row. Confidence: 0.6
- On full-screen video pages, the bottom bar (progress bar + input + like/fav/comment row) and the video area must be structurally separate, non-overlapping regions — the video area (with avatar/title overlaid at its bottom) occupies the space above the bottom bar, not overlaid on top of it. Use a Column with the video area as a weight(1f) block and the bottom bar as a sibling block below it, rather than a Box with align(BottomCenter) overlay; otherwise the avatar/title content gets squeezed or overlaps the bottom bar. Confidence: 0.75
- The user will reverse their own prior UI-change requests (e.g., "把你刚改的代码改回来" after asking the video feed to match the detail page's transparent rows/columns + separate bottom bar), restoring the affected screen's original layout (the feed's single bottom info block with a semi-transparent black background, Color.Black.copy(alpha≈0.3), and a spacedBy(20.dp) interaction row). Treat cross-screen UI unification requests as provisional and keep them cleanly reversible to the exact prior state. Confidence: 0.6
- On full-screen media pages, tapping the comment input must open the keyboard and KeyboardInputBar together, adding a scrim/遮罩 over the content; tapping the blank scrim area dismisses both the keyboard and KeyboardInputBar simultaneously (never one without the other). Confidence: 0.7
- Full-screen media list/feed pages (e.g., the Douyin-style video carousel) should be self-contained: interactions like viewing/adding comments happen in-place within the current page rather than navigating to a separate detail screen. The feed page and the dedicated video-detail page are distinct pages that merely share the same backend post data, so list items should not redirect to the detail page. Confidence: 0.7
- On full-screen video pages, keep the overlaid info minimal: show only the title (and author), dropping the body text and the "time · IP · views" footer row — the user explicitly asked to remove the time·IP row from the video detail page. Confidence: 0.7
- On full-screen/dark video pages, the bottom comment input box should be dark-adapted (适配黑色): a semi-transparent light background (e.g., Color.White at ~0.12 alpha) with muted-gray icon/placeholder text (e.g., #9A9A9A) following Xiaohongshu's dark-page palette, rather than the light-theme outline/secondary color tokens. Confidence: 0.65
- The home page top bar is a 关注/发现 tab pair: tapping 关注 filters the post waterfall to show only posts authored by accounts the current user follows, while 发现 shows the full feed; switching tabs must reload the list accordingly. Confidence: 0.7
- When wiring navigation from a list/notification item to its content, prefers reusing the already-built photo-detail and video-detail screens (and their existing navigation routes) rather than creating a new navigation target or a new detail screen. Confidence: 0.7
- Unread indicators on notification/message items should clear (mark as read) once the user taps through to view the item — marking read is triggered by that navigation, not by a separate manual action. Confidence: 0.6
- In notification/message list rows, the right-side square preview should reflect the actual content type: image posts show their real image cover, and video posts should show the real video cover thumbnail (the actual first frame, extracted via the same VideoThumb/MediaMetadataRetriever component video cards use) rather than a custom-drawn video icon/placeholder; video rows still route to the video detail page. Confidence: 0.75
- Account-scoped social state (likes, favorites, follows) must be keyed by the stable account uid, not the changeable display xhs_id, and the code + backend schema must use the same field. When such an action silently fails to persist (e.g., a tapped like never shows a filled heart), the assistant should check for schema drift — the table missing the uid column the code writes, or a legacy NOT NULL column the code no longer populates — because a schema/code field mismatch makes the write fail silently and the UI state never reflects the action. Confidence: 0.6
- When writing rows to Supabase REST, include optional fields only when they have a non-empty value (e.g., only send `media_url` when a media URL actually exists) — unconditionally sending an empty string for a column the live table doesn't have makes Supabase return a 422, so the row never persists and the local optimistic UI state (which looks fine) vanishes on reload; when a write "succeeds" locally but the data doesn't survive a refresh, verify the exact fields the code sends against the live table schema. Confidence: 0.6
- When tapping any input box (comment/chat/input fields anywhere in the app), the keyboard and the input bar/area above it should appear at the same time — the input bar should auto-focus (requestFocus via a LaunchedEffect) as it becomes visible so the IME rises together with the bar, rather than showing the input area first and the keyboard a moment later ("不要先弹上面的区域再弹键盘"). Confidence: 0.7
- Comment and reply image attachments must be persisted to the cloud (a comments-table image_url field) and rendered consistently across every surface that shows that comment — post/video detail, the profile "我的页面" own-comments list, and after tapping through to the post — not just displayed locally in the composing screen. If the same comment's images show in one place but not another, treat it as a persistence/schema bug (the image was never uploaded/stored), not a rendering-only issue. Confidence: 0.7
- Notification/message list items that reference a specific comment (e.g., a "新增评论" new-comment notification) should deep-link on tap to that exact comment: carry the commentId through the notification model and navigation, and have the target detail screen auto-scroll/locate the referenced comment — not merely open the post at the top. Confidence: 0.7
- When a comment deep-link points to a second-level reply (二级回复), the target screen should locate and highlight the reply's parent first-level comment (the scrollable row containing it), since replies are nested under their parent rather than being independently addressable list items. Confidence: 0.7
- When a notification/message item references a comment that has since been deleted, prefers showing a muted "该评论已删除" placeholder (without surrounding quotes) in place of the quoted comment text, and disabling the tap-to-locate/deep-link to that now-missing comment. Confidence: 0.7
- In notification/message list rows (新增评论/赞/收藏), the entire row should be one tap target that navigates to the content detail — not just the small preview thumbnail — while the avatar/username inside the row keeps its own tap action (open the user profile) via nested clickable precedence. Confidence: 0.7
- In tappable list rows (e.g., the 新增评论/赞/收藏 notification rows and the 消息/会话 conversation list), tapping anywhere on the row should give ripple feedback (default clickable indication) — do NOT suppress it with `indication = null` on the outer row clickable; the avatar/username keep their own inner tap (and ripple) that navigates to the user profile, and inner clickable precedence means tapping them doesn't trigger the row's navigation. Confidence: 0.8
- When auto-scrolling to a deep-linked target (e.g., the specific comment a notification points to), prefers a transient visual highlight on that element — a subtle tinted background (e.g., primary at low alpha) that auto-clears after ~0.5s (delay(500)) so the user can spot which item was located; the user set the ripple/highlight duration to 0.5s and expects the video-detail and photo-detail pages to use the same duration for consistency — and also specified the same 0.5s locating ripple when a message-search result jumps into a chat conversation scrolled to the found message. Confidence: 0.85
- In reply/mention input fields, the "回复 xxx：" prefix must behave as a protected region for cursor/edit purposes: the cursor always sits at the very end of the full text (moving forward with each keystroke as the user types, NOT fixed at the prefix end), clicking anywhere inside the prefix snaps the cursor back to the end, and the user cannot move the cursor into the prefix or edit its characters. But the prefix must NOT be indelibly locked against deletion: backspacing through it should cleanly exit the reply state — the prefix remnant is stripped so the input becomes an ordinary (non-reply) comment and the parent reply-target state is cleared — because trapping the user in reply mode until they send a reply was reported as a bug to fix ("可以删除 回复XXX: 就是评论,但是不可以移动光标到回复XXX里面"). Confidence: 0.85
- Relative timestamps on notification/feed surfaces should update in real time as time passes — a freshly arrived item shows "现在" and then automatically becomes N分钟前/N小时前/N天前/N月前/N年前, recomputed against the current time rather than frozen at the value rendered when the list first loaded. Confidence: 0.65
- Interaction notifications (赞/收藏/评论/关注/私信) must reach the user even when the app is backgrounded or the screen is off, delivered as real system notifications (heads-up banner for ~5s then into the system tray). Accepts a low-priority persistent foreground service (with a quiet 常驻 "正在运行" notification) that keeps the realtime connection alive as the means to achieve background delivery, and accepts that system tray card styling/time display cannot be fully customized by the app. Confidence: 0.6
- Account-scoped settings (e.g., notification switches) should be persisted per-uid in the cloud AND cached locally, merged by a version counter so the newer write wins — settings must survive reinstall/device switch and never regress from a stale overwrite, and multi-account values must stay isolated per uid. Confidence: 0.6
- When a cloud write is triggered from a composable (e.g., toggling a setting that PATCHes Supabase), the user's data shows the write never lands in the DB if the coroutine is launched in the page-level scope (`rememberCoroutineScope`), which gets cancelled when the user leaves the page right after toggling — so the persistence logic should live in a ViewModel (`viewModelScope` survives page exit) and the screen should surface an explicit save-result toast (成功/失败) so the failure is visible instead of silent. Confidence: 0.5
- When a cloud-persisted setting looks correctly applied on the owner's own screen (e.g., the profile lock icon, served from the local cache) but another account still sees the old/default state after switching, the user reports the divergence as a bug; the expected diagnosis is a silently-failed cloud write — typically the required ALTER-TABLE SQL adding the new columns was never run on the live Supabase table, so the write errors and only the local cache updates while other viewers read cloud defaults. The fix should make the cloud write's failure observable (log on failure rather than swallow it in a bare catch, giving the user an adb logcat tag to check) and have the user run/verify the migration SQL — not rework UI logic that already works. When the user reports they DID run the SQL and the columns exist, yet the DB row still shows the default value after toggling, the diagnosis must move into the write path itself (e.g., the save coroutine tied to the composable scope being cancelled on page exit before the PATCH completes, uid/row mismatch, RLS rejecting the PATCH) and be surfaced with observable per-save success/failure feedback (a toast plus a log tag) — not dismissed as a setup miss or a re-run of the SQL. Confidence: 0.65
- Bug reports arrive as full raw unfiltered logcat dumps — both when the assistant asked for a filtered capture (adb logcat -s <Tag>) and unprompted alongside a crash — dominated by emulator/system noise (vulkan/surfaceflinger/HWComposer/IME/documentsui; the test environment is an AOSP-style x86 emulator, MuMu/Nemu with houdini translation), usually without the requested tag's lines, introduced by a terse one-line symptom ("显示的已保存但是还是看的见", "左右移动照片没有移动效果,并且移动了再添加照片直接闪退了") and sometimes closed with "这是报错信息". The meaningful lines are buried mid-dump: the app's own network lines (Volley "Unexpected response code ...") and its AndroidRuntime FATAL EXCEPTION + Compose stack trace (e.g., "Key ... was already used ... LazyColumn/Row ... provide a unique key for each item") — the assistant should extract those rather than treat the noise as relevant. A dump can also be PURE emulator/system noise with zero app lines, pasted with only "这个报错是什么" — the assistant is expected to categorize each noise line and dismiss it as irrelevant rather than chase a non-bug. The known non-app noise families: (1) a MuMu 网易 watchdog process (com.netease.nemu_vapi_android) logging "upload event: ... libhoudini.so ... LaunchAPP ... error code 0/ok" — the emulator reporting a successful app launch, houdini/x86 translation being normal; (2) `libc hwservicemanager` "Unable to set property ... ctl.interface_start ... graphics.mapper/allocator ... error code: 0x20" — system HAL init permission denials; (3) `OMXNodeInstance ... OMX.google.decoder.avc ... UnsupportedIndex/UnsupportedSetting` — the media decoder querying optional extension configs, benign (query-failure, not decode failure) as the codec falls back to software decoding. When the dump is all noise, the assistant redirects the user to the app's own tag (e.g., `adb logcat -s RedBook`) to actually debug. Confidence: 0.72
- Persistence bugs in this stack are diagnosed from the status codes in the app's REST/Volley log lines, not from success toasts: a PostgREST SELECT naming a nonexistent column returns 400 (e.g., on notif_*/privacy_* columns), a query on a nonexistent table returns 404, and a PATCH that names nonexistent columns returns HTTP success while updating 0 rows — so a "已保存" toast is a false success. Cloud writes that must be trustworthy should be followed by a readback of the row from the cloud (save-then-verify), and when a 400 appears the live table's actual columns should be confirmed via information_schema even if the user previously reported running the migration SQL. Confidence: 0.6
- Notification/alert settings follow a master-switch + per-category model: every category defaults to ON (opt-out), each category switch gates only its own event type, and switching the master OFF disables/greys out all categories while preserving their remembered values so re-enabling restores them; system notification permission must be granted before any of the switches can take effect. Confidence: 0.55
- Notifications must cover offline gaps, not just real-time pushes: the user expects an event that occurred while the receiving account was logged out / not subscribed (e.g., on one emulator, account A sends a DM then logs out and account B logs in afterward) to still pop up after login — the assistant's fix is an offline catch-up on service start/account switch that queries unread notifications/messages since a per-account "last notified" watermark and re-posts them, reconnecting the websocket when the uid changes and advancing the watermark only when a notification actually posts (never while system notification permission is missing, so a later grant re-delivers the backlog; first use back-fills only ~24h to avoid spam). Confidence: 0.7
- Notification deep-links must carry enough data to fully render the destination page's user identity: tapping a DM notification into the chat is expected to show the peer's avatar (top bar and message bubbles), not just the nickname — the notification payload must include the actor's avatar_url alongside the name, and when the payload lacks it (older notifications, failed lookups) the app should fetch the avatar by uid as a fallback before/while entering the page instead of leaving the avatar blank. Confidence: 0.6
- Enforcing "all app surfaces show my remark instead of the user's name" is expected to be implemented at the data-assembly layer: parse the raw cloud rows with their uid (author_uid/actor_uid/userId — adding the uid field to models where missing), batch-fetch my remarks for all uids at once (one getRemarks-style query), then swap each display name to remark.ifBlank { original } before handing models to the UI — so home/search/browse feeds, post/video detail authors, comment/reply authors, and profile post cards all get remapped without touching every leaf composable; name-only denormalized rows without any uid are the only accepted exception. Confidence: 0.7
- The user identity model must include the Xiaohongshu numeric ID (xhs_id) as a first-class searchable identifier: add-friend/user search matches EITHER the display name (nickname) OR the xhs_id (server-side fuzzy/ilike over both), and result rows show the name (remark-preferred) with a secondary line in the form "小红书ID：xxx". Confidence: 0.6
- When a new page must behave identically to an existing page except for one label (e.g., add-friend page vs message-search page: "页面padding和顶部的搜索row逻辑和搜索页面的一模一样,只是提示字变成添加好友"), the shared piece (the top search row) should be extracted into a single reusable composable parameterized by the placeholder text, with both screens using it — not duplicated per page. Confidence: 0.6
- Follow-state chip on friend-add/contact-search result rows (transparent background, 16dp-rounded outline border, fixed width with centered label): unfollowed shows 关注 with primary border+text, followed shows 已关注 with getOutline border+text; toggling updates optimistically and persists to the cloud. Confidence: 0.65
- Location/IP labels shown to users (profile header, post-detail "time · IP · views" footer, comment/reply "· IP") must display only the province short name, e.g. "湖北" — no 省/市/自治区 suffix and no city-level detail; richer geo results are expected to be normalized ("湖北省"→"湖北", "广西壮族自治区"→"广西", municipalities like 北京 keep the short form) before display. Confidence: 0.7
- When a "who published this content" IP label (post-detail author IP, etc.) is blank because the content predates IP capture, the fallback must be keyed to the content AUTHOR, not to the current viewer: derive the effective IP by checking the author's own stored users.ip_location (or the viewer's IP only when the viewer is the author), so the label still renders when a second account views another account's older posts — viewer-scoped fallbacks (authorId == userUid ? myIp : blank) hide the label across accounts and are treated as a bug. Confidence: 0.7
- When acquiring location-based display data, prefers a dual-source strategy over a single source: use device/GPS location (so the emulator's virtual location takes effect) when the location permission is granted, and fall back to a public-IP geolocation API otherwise — the user picked this "定位+IP 双方案" combination over either approach alone, and rejected nothing silently: fields that can't be determined must not stay blank. Confidence: 0.6
- At display spots for real-world data, showing the placeholder "未知" or an empty rendering ("IP：" with nothing after, or a dangling "· ") is itself reported as the bug — such spots should either show the real value or render nothing (hide the row/segment) rather than placeholder text. Confidence: 0.55
- A bug report that enumerates multiple surfaces of one broken pattern (e.g., "资料页的IP、卡片详情时间后的IP、评论的IP都没有显示") expects the fix to cover every occurrence of that pattern across the app (incl. replies, own-comments list, video pages), not just the first place named. Confidence: 0.5
- Display fields that show real-world data (e.g., the "IP：..." location footer on profiles/posts/comments) are expected to be backed by an actual end-to-end pipeline — the app really collecting/persisting the value (e.g., device or network location written into the cloud ip_location field) — never just display-layer reads of a column no code ever writes. When such a field shows blank, the user reports it as a bug and tries environment knobs (enabling the emulator's virtual location) expecting the app to react, so the fix must add the missing data acquisition, not dismiss the empty value as expected. Confidence: 0.6
- When told to enable environment prerequisites or run a diagnostic verification step for a fix (e.g., run a supplied SQL migration, turn on the emulator's virtual location, restart the app, or manually UPDATE a DB row to test a hypothesis), the user actually performs them and then reports the residual symptom in one terse line ("依旧没有ip显示", "还是可以看到") — they expect the fix to hold up in their actual environment (AOSP emulator without Google services, third-party API unreliability, etc.), so the assistant should keep debugging code-side failure causes (permission-request coverage, provider/Geocoder availability, real API reachability — verified by live-testing endpoints) rather than re-asking the user to repeat setup or treating non-compliance as the likely explanation; a terse residual report after a manual DB write succeeded moves the diagnosis to the app's read/viewer side (data loading, ViewModel key reuse, cross-account semantics), not the write path. This also extends to API-credential fixes: after the user reconfigures an external provider (e.g., fills a new Volcengine Ark endpoint ID into supabase.properties, rebuilds and reinstalls), they verify the app and report the residual user-facing failure in the same one-liner app-level symptom ("还是显示小助手暂时无法回答,请稍后再试") rather than the underlying HTTP status — they expect each downstream cause the assistant identifies (404 invalid model name → 500 endpoint on a decommissioned model → ModelNotOpen) to be chased until the app actually succeeds. Confidence: 0.75

older notifications, failed lookups) the app should fetch the avatar by uid as a fallback before/while entering the page instead of leaving the avatar blank. Confidence: 0.6

endpoints) rather than re-asking the user to repeat setup or treating non-compliance as the likely explanation. Confidence: 0.55

older notifications, failed lookups) the app should fetch the avatar by uid as a fallback before/while entering the page instead of leaving the avatar blank. Confidence: 0.6

- RLS check on this project's Supabase confirms two distinct `users` tables with opposite RLS states: the app's business table `public.users` has RLS DISABLED (relrowsecurity=false, holds xhs_id/nickname/ip_location/privacy_* columns) while Supabase's built-in `auth.users` has RLS enabled — so in this schema "anon PATCH blocked by RLS" is a dead hypothesis (as is "migration ran against the wrong table" once columns are confirmed present), and cloud-write debugging must move to the write request itself (columns named, uid/row targeted, HTTP method) or to the viewer/read side and cross-account semantics (which account toggled vs. which account views). Confidence: 0.7
- For cloud-save operations (settings toggles etc.), prefers zero UI interruption: explicitly requested removing the save-result Toast ("现在把Toast去掉,不影响观感") so the screen stays visually clean — the save is still verified by read-back from the cloud and any failure is written only to the log (Log.e under the logcat tag), never popped as a Toast; when removing it, also strip the Toast/result-message state plumbing (saveMessage/consumeSaveMessage) and the now-unused imports. Confidence: 0.85
- For the AI 小助手 feature, prefers a "双保险" (dual-provider fallback) architecture so a @小助手 reply is answered by whichever provider works: vision models (豆包视觉, used when the post/media present) and a text model (DeepSeek文本) both serve as fallback for each other — if the primary provider fails/times out (HTTP 5xx, ModelNotOpen, timeout), the code automatically retries the other so a question still gets a normal answer as long as any configured key is functional; both chains (with media and without media) must be able to ask and answer normally, and the fallback is not a degraded "can't answer" path. Confidence: 0.7
- The "回复 @小助手：" AI prefix in the comment input must follow the exact same deletable-but-protected semantics as the regular "回复 xxx：" reply prefix: while typing, the cursor stays at the end and the prefix itself can't be edited, but backspacing through it must NOT be ignored/blocked — instead the prefix remnant is stripped and the input cleanly exits AI/mention mode and becomes an ordinary first-level comment (the user reported once the prefix appeared it was "没办法删除" and demanded it behave "就像删除回复前缀一样"). A prefix that is indelibly locked (input silently ignored when the prefix is broken) is a bug. Confidence: 0.75
- For AI 小助手 conversation continuity: an AI reply expects to support multi-turn 追答/追问 — when the user replies under the assistant's comment, the reply content must carry the "回复 @小助手：" prefix (displayed and persisted) so the system can recognize it as another @小助手 question to the same thread and the assistant answers again with the prior conversation as context; this prefix + continue-answering contract must be applied consistently on every surface that lets the user reply to the AI (post detail, video detail, video feed comment threads). Confidence: 0.65
- Requests for UI affordances name the exact drawable asset to use (e.g., "close_ring_fill.xml" for the photo delete button) and expect that specific existing resource wired to the control — implement with that named asset, not a look-alike or a freshly-drawn substitute. Confidence: 0.65
- In the publish (发布) multi-photo flow, expects Xiaohongshu-style management of the already-selected photos rather than a static thumbnail strip: each photo shows a delete button at its top-right corner that removes that single photo, and the image order must be user-adjustable by drag-and-drop (implemented as long-press-drag with live reordering in this case). Confidence: 0.6

- When implementing drag-reorder UI, expects the dragged item to visibly follow the finger (跟手) during the drag; a fix that produces no visible movement during the drag is reported back as a bug ("没有移动效果") regardless of whether the final order changes. The end-state target is Xiaohongshu-style live shifting: after trying a stable drop-at-end build, the user explicitly demanded that the remaining photos move along in real time as one is dragged ("移动了一个照片剩下的照片也要跟着移动,就像小红书拖动逻辑") — dragging across half a slot width should swap with the neighbor immediately with a smooth slide animation, so the whole row lanes/shifts during the drag rather than reordering once on release. (A stable interim build where only the dragged item follows and reorder happens at drop is acceptable as a stopgap, but the user will then ask for the live-shift version.) Confidence: 0.75
- Treats the user's repeated-same-photo picks as an expected real input, not an edge case: when a crash occurs on re-adding an already-selected image (Compose "Key was already used" from keying LazyRow items by content uri), the assistant must fix by not keying items on a value that can repeat (or otherwise ensuring uniqueness), and should prefer index-based removal (removeImageAt) over uri-based filtering so duplicate uris can't delete the wrong image. Confidence: 0.7
- For Compose live-reorder (小红书-style) drag lists, expects the implementation pattern that keeps drag gesture continuity while items reflow: dedupe repeated items in the add path so item keys are unique; key LazyRow items by the stable identity (uri) and key the per-item pointerInput on that same identity so the gesture block is not torn down when the item's index changes during a swap; track "which item is being dragged" by identity (draggingUri) rather than by index; and read the item's current index inside the gesture through rememberUpdatedState so the swap logic always uses the fresh position. Item placement animations (animateItem) then make the neighbors slide out of the way smoothly. Confidence: 0.8
- Editing an already-published post should reuse the app's existing publish/create screen in an edit mode rather than a new editor: tapping 编辑 re-enters the publish page ("该帖子重新进入发布也编辑"), which pre-fills title/content/images from the post, relabels the primary button 发布笔记 → 保存修改, and saves via PATCH that preserves the original row's id/created_at (existing cloud images kept untouched, only newly-added local images are uploaded) — the same reuse-the-create-flow pattern already used for editing drafts; this reuse applies to video notes as well (edit is carried from the video detail page into the same Publish edit mode via a shared editPost/PostToEdit slot). Confidence: 0.7
- A content-detail screen reachable both as one's own content (from 我的-笔记) and as other users' content (home/search/notifications/other profiles) needs the bottom bar's role switched by ownership/entry: the owner/author entry replaces the comment input bar with a management "编辑区域" (visibility label row + 编辑和权限设置 entry, whole area clickable) that pops up a bottom panel with 编辑/权限设置/删除; non-owner entries keep the normal comment input bar — so all non-owner navigation paths should explicitly enter viewer mode. Confidence: 0.55
- On a detail page reachable from many entry points with a caller-supplied "editMode/author mode" flag, prefers guarding the author-edit UI by an ownership check at runtime (authorUid == userUid from cloud data) rather than trusting the flag alone — the flag is only set by the correct entry (own-profile cards), and if it ever arrives true for a non-author, the page should degrade gracefully to the normal viewer bottom bar instead of showing author controls to the wrong user. Confidence: 0.6
- When introducing new per-screen navigation state that must be explicitly false for most callers (e.g., an author-edit flag on a video-detail route, true only for the own-profile entry), enumerates every navigation entry point to the screen and explicitly resets the flag to false on all non-owner paths (home feed, peer profile, browse, messages, comment/notification deep-links) — leaving any path unpatched would carry a stale true from a previous visit into a non-owner view. Confidence: 0.6
- Image notes and video notes have separate detail pages in this app (DetailScreen for image notes vs. a full-screen dark VideoDetailScreen for video notes, whose row lives in the same posts table with a `video:`-prefixed image_url) — when a detail-page feature is requested for "笔记/帖子详情页" (e.g., the author 编辑区域 bottom bar + 编辑/权限设置/删除 panels), it is expected to work on BOTH note types. Adding it only to the image-note detail screen will come back as a one-line bug report ("视频笔记底部bar没有切换编辑区域成功"), and the fix must apply the same edit area/panels to VideoDetailScreen and route the user's own video notes (profile 笔记-tab video cards) into that page in author/edit mode, with all other video entry points explicitly viewer mode and a refresh key that reloads title/visibility after an edit-save return. Confidence: 0.85
- Modal bottom panels/action sheets (e.g., the author 编辑/权限设置/删除 panels and permission panels on the note-detail bottom bars) should be presented as topmost-layer overlays rather than embedded inline in the bottom-bar slot: a full-screen semi-transparent scrim covers the page content (the card detail stays visible dimmed below, "底部才是卡片详情"), the sheet is bottom-anchored above it, and tapping the scrim dismisses it; the sheet card itself gets top-only 16dp rounded corners + soft shadow + surface background, with bottom content padding/navigation-bar clearance so it doesn't sit under the gesture bar. Both the image-note and video-note detail pages share this same overlay/sheet presentation. Confidence: 0.75
- Settings pages with a list of toggle rows (e.g., privacy settings switches like 展示笔记/展示评论/展示收藏/展示赞过) should not wrap the rows in one big rounded container Column: each row should be its own independent card — own ~15dp rounded corners + soft shadow + surface background — with ~10dp spacing between rows. Confidence: 0.75
- Settings sub-page headers (返回 icon + page title, e.g., 隐私设置/通知设置) should be truly centered: title absolutely centered over the full row width while the back icon stays at the start (Box with icon aligned CenterStart and title centered via align(Center).fillMaxWidth()) — placing the icon in a Row and letting TextAlign.Center center only within the remaining space reads as off-center. Confidence: 0.7
- On the dark full-screen video detail page, the author edit area over the black video must be color-adapted for readability: the first row (visibility lock icon + text) uses surface(white) color and the second row (编辑和权限设置) a semi-transparent surface — light-theme tokens like onSurface/onSurfaceSecondary are unreadable on black, so dark-adaptation logic differs between the video page (isOnDark/surface-based) and the light image-note page (onSurface/secondary-based). Confidence: 0.7
- When implementing/reworking a mode-specific feature (e.g., the author editMode bottom bar on a post detail page), existing visible UI elements must not be silently hidden or gated off by the change — the assistant had wrapped the avatar + "说点什么" CommentInputArea row at the top of the comment list in `if (!editMode)` to avoid duplicating the new author edit bottom bar, and the user immediately reported the regression entering their own photo post from 我的 ("那个有头像和评论输入框的评论row怎么不见了,给我加上") and expected it restored to always-visible. A previously-present element must keep appearing in every mode unless the user explicitly asked to remove it; restoring means removing the conditional gate, not relocating it. Confidence: 0.65
- Project documentation (README etc.) is expected in Chinese, concise and high-level ("大致总结不需要写的很细"), not exhaustive: use markdown tables for the tech stack and for a feature↔implementation correspondence table ("功能和所用东西一一对应不用写太多"); must cover the language used, architecture, the network protocol section (this project: HTTPS/REST for data + WebSocket for realtime), and a brief backend (Supabase) overview — with the main emphasis on the app's features and how they work — and the content should reflect the actual codebase (dependencies read from libs.versions.toml/settings/manifest, real screen/module names) rather than generic boilerplate. Confidence: 0.7
- For 答辩/defense-preparation deliverables (project study guides for a 项目验收答辩, e.g., a "项目全解与答辩指南" markdown saved at the project root), the user explicitly wants the OPPOSITE of the concise README style — exhaustive, detailed coverage ("总结出来,要求详细,让我掌握清楚整个项目"): every feature mapped to its implementing code (file paths, key functions, data flow), a layered knowledge-point list (Kotlin/coroutines/Compose/system/network/database), architecture diagrams, the database design, and anticipated defense Q&A — this is a distinct documentation context from the concise high-level README preference and is expected written in Chinese. Confidence: 0.5
<<<<<<< HEAD
- Login state must be remembered persistently: once logged in, the session survives clearing the app from the background and process death/restart ("不管是清除后台还是切换进程,都保持登录状态"), and the only two things allowed to end it are the user explicitly logging out or reinstalling the app — so the app should persist the logged-in user locally on login success, restore it on cold start (skip the login screen and land on Home directly), and clear it on logout. Confidence: 0.5
- Launcher icon composition: the central glyph must not fill the whole icon — when the inner asset is scaled up to ~61% of the 108dp launcher canvas the user reports it as "占满了整个图标,太大了"; the fix is to downsize the center graphic to roughly 40% of the total icon (e.g., 44dp of 108dp), centered on the background layer, which reads as the appropriate/suitable size (the user cites the source asset's native size, e.g., appicon.xml at 58dp, and expects the sizing rationale anchored to those dp values). Confidence: 0.55
=======
>>>>>>> 0c73c2bc6498d1bfc6e0630fcfcf431a7ede233a
- When adding a virtual/system persona that appears in user-generated content surfaces (e.g., the AI 小助手 that answers "@小助手：提问" comments in the comment section), expects it as a distinct, consistent identity with an explicit presentation contract: fixed reserved uid + username (小助手), the username always rendered in the theme's brand blue getBlueFill() wherever it appears (the user stressed "始终为getBlueFill()", covering both top-level comment items and nested reply items, not just the input mention), a dedicated avatar (originally specified as the app's icon composition at an appropriate size — when the existing icon can't serve as a round avatar, confirm the avatar approach before inventing one), display-only behavior (name/avatar not tappable and no profile navigation — the user chose "仅展示不跳转" over making it a fake real user), and replies persisted to the cloud in the same table/structure as real user comments (user: "并且也要存入云端") so they render everywhere real comments do; the persona's fixed name must also be exempt from per-user remark overrides. Confidence: 0.6
- When a persona/identity visual treatment (e.g., 小助手's blue username) is requested as "全局都要有", it means exhaustive coverage of every surface where that identity's name can appear — not just the shared comment components. The user demanded the blue styling globally after it had only been applied in CommentItem/ReplyItem; the resulting sweep extended to the profile 我的评论 list, where a "@小助手：…" quoted-mention prefix (the assistant as the replied-to parent comment) is colored blue via an added uid check, and the data layer was extended (parent_user_id / parentUid) to make that possible. The assistant is expected to search all rendering sites (comment rows, nested replies, @mention/quote prefixes, notification actor names, own-comment lists) for the identity's uid/name rather than assume components cover it; surfaces whose data source structurally cannot contain the identity (e.g., notification/chat lists built from real users-table rows) may be excluded after verifying that. Confidence: 0.8
- Blue-highlighting an @mention token in text content (e.g., the literal "@小助手" inside a comment's body text or inside the comment input field) is a SEPARATE requirement from blue-highlighting the persona's author-username row. When the user asks "@小助手的字体颜色为getBlueFill()" and later reports "@小助手没有蓝色" after only the author-name row was colored, they mean the "@小助手" string itself — wherever it appears as text: typed in the input box (visible in blue while typing, via AnnotatedString in the TextField value) and in the published comment/reply body reading "@小助手：问题" — must render in the brand blue, in addition to (not instead of) the blue username line. The mention highlighter should be a shared helper reused by the input field, top-level comment bodies, nested reply bodies, and own-comment/profile list bodies. Confidence: 0.7
- When a feature is fully implementable with zero database schema change (e.g., an AI persona's replies written as ordinary rows into the existing comments table, no new table/column/trigger), the assistant should state explicitly that no SQL migration is needed and that local Gradle config files (supabase.properties and its KEY=value lines like SUPABASE_SERVICE_ROLE=...) must never be pasted into the Supabase SQL editor — the user twice ran the properties file's contents as SQL and reported the resulting 42601 syntax error back as if it were a feature bug; the assistant is expected to preempt that confusion by clarifying the config file is local-only and that the only activation step is editing the local file and rebuilding. Confidence: 0.55
- When a cloud/network feature fails at runtime even though the user has configured the needed credentials (e.g., "显示无法回答,为什么,我不是有豆包的api吗"), the user expects the assistant to trace the full chain rather than assume the config is wrong: first verify the config (key/URL/model in supabase.properties) actually reached the built APK via the generated BuildConfig, and only once injection is confirmed, isolate the failure to the runtime request path using device logs (adb logcat filtered to the app's tags) and screenshots rather than re-asking the user to reconfigure or repeating setup steps. Related tooling fact: after the user edits a value in supabase.properties that feeds a buildConfigField, the `:app:generateDebugBuildConfig` task often reports UP-TO-DATE and does NOT rewrite BuildConfig with the new value — the assistant must force-regenerate it (e.g., `--rerun-tasks`) and confirm the new value is present in the generated BuildConfig.java before a normal assembleDebug will ship the updated field, otherwise the freshly built APK still embeds the old value. Confidence: 0.6
- Editing an already-published post and choosing "存草稿" (save to draft) must MOVE the content wholly to the draft box, not leave it in both places: the user reported a bug that a published post edited and stored to draft still showed in the sent/published list as well as the draft box ("发送的帖子通过编辑存入草稿还是会存在于发送的帖子里而不是只存在于草稿箱"). Expected behavior: the draft-save flow for an editing-published-post case must handle the `editPostId != null` branch (which plain new-draft saving otherwise ignores) and delete the original published post once the draft is saved. Confidence: 0.6
- The "preserve cloud media, only upload newly-added local media" rule that governs the publish-edit path applies equally to the save-as-draft path: `saveDraft()` on an edited published post must detect that the state's media URIs are already remote URLs (calling the same `isRemoteImage()` check the publish path uses) and keep the remote URL verbatim (including a `video:` prefix for videos) instead of trying to re-upload/cache the remote URL as a local file — failing that re-upload produced an empty imageUrl so the saved draft card rendered the `test` placeholder image; only genuinely new local media gets uploaded/cached. Confidence: 0.7
- The "preserve already-cloud media, only upload newly-added local media" rule applies not only to editing, but to publishing a saved draft as well: a draft's media are already remote URLs (image `https://...`, video `video:https://...`), so `publish()` of a draft must reuse them verbatim rather than hand every media URI back to `uploadImage()` as if it were a fresh local file — re-uploading remote URLs fails (contentResolver can't open an `https://` stream) and surfaces an "上传失败" toast. Related robustness requirement: a draft's image is cached to a `file://` URI on save, so `uploadImage()` must also handle the `file://` scheme by reading the file directly from disk (inferring MIME from the file extension via a helper, since contentResolver.getType returns null for `file://`), not only `content://`. Confidence: 0.7
- When an edit action deletes or moves the edited entity (e.g., editing a published post then saving it as a draft deletes the original published row), returning from that screen must NOT navigate back to the now-deleted entity's detail page (which becomes a "post not found" blank/detail) — instead it should pop multiple levels back to the originating source page (e.g., the profile/我的 page that listed the post). Because the return target depends on which final action ran, the screen's completion callback should carry which action occurred (a `savedAsDraft` flag) so the caller can decide between one-level `goBack()` (edited content retained: back to its detail) and two-level pop (content moved/deleted: back to source list). Confidence: 0.65
- On the profile stats row (关注/粉丝/获赞与收藏), each stat is independently clickable and navigates to its own destination: tapping 关注 opens the "following" (我关注的人) list screen, tapping 粉丝 opens the "fans" (关注我的人) list screen, and tapping 获赞与收藏 pops a centered dialog showing the user's actual like-count and favorite-count totals pulled from the cloud in real time. The following/fans list page reuses one shared screen/viewmodel parameterized by mode (FOLLOWING vs FANS) with a top back bar (arrow_left icon + centered bold title + getOutline-at-50% divider) and rows of 48dp round avatar + name (remark-preferred) + right-side follow chip; tapping the avatar/name/row (excluding the button) navigates to that user's profile. Confidence: 0.7
- Summary/aggregate numbers shown beside their detailed breakdown must be internally consistent: where a stats header item (e.g., the profile row's 获赞与收藏) sits next to a popup dialog that breaks it into components (获赞/收藏), the header number should equal the sum of the breakdown components so they "对得上" — displaying a header count derived from only one component (e.g., just favorite count) while the dialog's first line shows another (like count) is treated as a mismatch to fix, not left as-is. Confidence: 0.6
- In following/fan-list rows, the right-side follow button has a per-state contract with cloud sync: in the FOLLOWING (我关注的人) list every row shows either 互相关注 (border getOutline, text getOnSurfaceTertiary, transparent bg, no ripple) or 已关注; in the FANS list a user I haven't followed back shows 回关 with primary border+text, and a mutual shows 互相关注. Tapping an already-following (已关注/互相关注) button opens a centered confirmation dialog titled "不再关注作者?" (bold onSurface, centered) with 取消 (left) and 不再关注 (right, primary) separated by a vertical divider, the row bounded by dividers also at getOutline 50% — the user must confirm before unfollowing, and the resulting state change syncs to the cloud and to the global app. Confidence: 0.6
- For background realtime notification delivery (system messages — likes/favorites/comments/follows/DMs — via a foreground NotificationService + WebSocket subscribing cloud notifications/messages by a recipient uid filter), the subscription account must ARRIVE with the service launch and be favored over any device-local cached identity. The cross-device bug was that the service read "current account" from device-local SharedPreferences (the last/old uid cached on that device), so messages received on the SAME device worked after switching accounts (the uid-change triggered a reconnect to the new account) but were NOT received on a DIFFERENT device (the service subscribed to the stale device-cached old uid and the recipient filter never matched the interactor's target). Expected diagnosis/fix: pass the current logged-in userUid into the service via an explicit Intent extra (EXTRA_UID); the service prefers that uid for subscribing and filtering, syncs the local cache when the intent uid differs (so a START_STICKY restart won't re-subscribe a stale account), and only stops itself when neither source has a uid. When the user reports "same device switching accounts works, but different devices can't receive system messages," the rootcause is this device-local-uid-source problem, not the realtime/websocket plumbing itself. Confidence: 0.65
- When a user asks for a vertical-padding change on a control (e.g., the "我知道了" dialog confirm button), they distinguish WHERE the padding lives: "不是按钮内部加20dp的padding,是row上下加20dp的padding" — padding belongs on the surrounding Row/container (providing the outer spacing from the content around it), not grafted onto the button's internal padding that sets its height; a prior edit that moved the padding inside the button was corrected by restoring the button's internal padding to a reasonable height and putting the outer 20dp row vertical padding back on the container. Confidence: 0.6
- When asked to speed up a slow upload of large media (e.g., big videos), prefers streaming the file to the server rather than loading it whole into memory: the previous path read the entire video with `readBytes()` into memory then handed it to Volley's `StringRequest` (slow and OOM-prone), so the fix switched video uploads to an OkHttp custom `RequestBody` that streams in chunks (64KB buffer) from content:///file:// while holding constant memory, and raised timeouts (connect 30s / write 10min / read 10min) so big uploads don't time out mid-flight — routing only the small-image path through the older Volley read-bytes route. When a capable library for this is already in the project (OkHttp via the existing WebSocket), reuse it rather than adding a new dependency; also prefer the non-deprecated `mime.toMediaType()` extension over `MediaType.parse`, define the buffer constant explicitly (DEFAULT_BUFFER_SIZE isn't auto-imported), and verify the deprecated/untitled API against the actual library version before compiling. Confidence: 0.65
- When small-file and large-file paths diverge (image vs. video upload), prefers branching on MIME type so each path uses the right strategy — streaming for large/lossy files (videos), the simpler read-bytes path for small ones (photos) — rather than forcing both through one approach. Confidence: 0.55
- For list surfaces representing a relationship that the user removes in place (e.g., unfollowing someone in the "我关注的人"/FOLLOWING list), the item must be removed immediately from the list UI — not merely have its follow button toggled — while the stat counter (关注数) updates at the same time; the user reports as one bug that "取消关注列表没有立刻刷新删除这个列表项,而我的页面的关注数确实时刷新了". The two-list semantic is: in the FOLLOWING list, unfollowing deletes the row outright; in the FANS/粉丝 list (which lists followers, a different relationship), unfollowing must keep the user in the list but return the button to 回关. Confidence: 0.7
- When logging in with an ACCOUNT name (not email), the displayed account must be forced to the user's input value, not trusted from the backend Auth `user_metadata.account` — that Auth field can hold stale/corrupted/串号 data that differs from the real account (e.g., an account login for `snape123456` showed `snape23456` because that user's Auth metadata stored the wrong account), and relying on it produces a wrong account in the sidebar; only the email-login path should fall back to the DB `users.account`. Root-causing such a wrong-account display means checking whether the code trusts backend metadata over the actual typed identifier. Confidence: 0.6
- When a bug is caused by bad/stale data (e.g., a user's Auth `user_metadata.account` differing from the real account), the user may prefer to fix the data at the backend/database layer manually rather than patch the bug defensively in code — and will explicitly ask to REVERT the code-side workaround ("把你刚改的这个代码恢复成改之前的,我会手动改auth里的账号"), even when the code fix was correct and compiled. Holds the mental model that data lives in two independent Supabase copies (business `public.users` table vs `auth.users` `raw_user_meta_data` JSON) and that editing one must be synced to the other; expects the assistant to revert the code cleanly to its prior state and, if offering the code fix again, only re-apply it after the user asks — never keep a code workaround the user wants to replace with a manual data edit. When offered a concrete pick-one between data-fix directions (e.g., align the Auth `user_metadata.account` to the business `users.account` vs. align the business table to Auth), the user engages with a terse choice ("1") and accepts the assistant performing the direct backend write (service_role Admin-API PUT on auth.users metadata) — the durable stance is correcting the data at the backend layer (with the local app session cache cleared/re-login for the new value to show) rather than re-applying any code workaround, whether the user or the assistant executes the edit. Confidence: 0.7
- Reports slow media/video playback loading ("视频加载的有点缓慢") as a bug demanding a real fix rather than a "measured-optimum" explanation: expects the actual root cause to be addressed — e.g., swapping a plain system `VideoView` (no disk cache, no preload, conservative buffering) for Media3/ExoPlayer with a global LRU disk cache + CacheDataSource transparent proxy, preloading adjacent feed pages (beyondViewportPageCount), and aggressive start-buffering, so that first play is faster, swipes are near-instant, and re-watching cached videos is instant. Confidence: 0.55
- "Performance optimizations" that override tried-and-true library defaults or add download-size constraints are treated with suspicion and immediately flagged when they regress the UX: the user reported "优化了之后更卡加载更慢了" after the assistant added a global custom Coil ImageLoader (replacing the default OkHttp-backed loader, which broke local `file://` draft images), image-request `.size()` constraints (which conflicted with the content page's aspect-ratio logic that relies on the original intrinsic dimensions, causing relayout/decode churn), video-thumbnail timeout/caching state, and Volley retry policies. Because the changes were broadly spread across unrelated files and touched working defaults, the fix is a clean revert to the original settings (restore RedBookApp, remove the `.size()` calls, roll back VideoThumb/PostContent/PostCard), keeping only the narrowly-scoped behavior fixes the user explicitly requested — the default library configuration is treated as a known-good baseline not to be globally reconfigured for speculative gains. The user has re-confirmed this after a second failed attempt, explicitly noting "你刚才优化的跟没优化一样,更卡了" and that draft images "草稿箱图片也加载不出来了"; when they then re-authorize optimization they explicitly decree which already-fixed files (e.g., FollowListViewModel, NotificationService) are off-limits ("这两个代码不动") while authorizing the rest — a designated-files-off-limits list is expected to be honored strictly while pursuing the remaining scope. Confidence: 0.8
- Video sizing preferences are provisional and reversible: after an earlier request to unify all video surfaces to fill the width via `PlayerView` `RESIZE_MODE_ZOOM` + `fillMaxSize`, the user then reversed it ("把视频的尺寸恢复修改前的状态") and had the feed and detail players restored to the original `fillMaxWidth().wrapContentHeight()` with the default FIT (aspect-preserving, videos sized to their content box). The same reversal repeated on the next sizing change: after the assistant switched to `RESIZE_MODE_FIXED_WIDTH` to eliminate portrait-video side gaps, the user again reverted ("给我恢复改之前的") back to the default FIT/aspect-adapted baseline, tolerating the portrait gaps. Treat every video-sizing change as provisional and reversible to the original aspect-preserving FIT layout as the baseline to fall back to on any such revert request. Confidence: 0.7
- Portrait/vertical videos in a width-filling player must occupy the FULL width with no gaps on either side: the user reported left+right gaps on portrait videos and expects the fix to fill the width while keeping height aspect-adapted. In Media3 `PlayerView` the cause is default `RESIZE_MODE_FIT` combined with `fillMaxWidth().wrapContentHeight()`, which adapts by height and shrinks the view width for portrait aspect ratios — the fix is `AspectRatioFrameLayout.RESIZE_MODE_FIXED_WIDTH` (width always fills, height computed from the video aspect ratio, no crop, no letterbox), applied consistently to both the feed and detail players. Confidence: 0.75
- On width-filling video players, portrait/vertical videos should get a cover treatment while landscape/square keep the original aspect-preserving display — the user explicitly asked to "把竖屏视频宽度占满,高度居中裁剪为视频区域的高度" (portrait fills width, height center-crops to the video-area height), with landscape/square untouched. Implemented by reading the video's real aspect from `Player.Listener.onVideoSizeChanged`: when width < height (aspect < 1), switch the view to `fillMaxSize` + `AspectRatioFrameLayout.RESIZE_MODE_ZOOM` (height scales up and is cropped, centered); otherwise keep `fillMaxWidth().wrapContentHeight()` + default FIT. This split-behavior request supersedes earlier portrait fixes (FIXED_WIDTH no-gap, and the provisional ZOOM-then-revert cycle) — portrait videos now legitimately crop, and only the landscape/square path stays aspect-adaptive. Confidence: 0.7
- Video card cover thumbnails (封面) should be cached rather than re-extracting a frame via `MediaMetadataRetriever` (which re-downloads the video head over the network) on every recomposition: expected as a two-level cache — in-memory LRU (recent/back-scrolled cards show instantly) plus disk JPEG persisted under cacheDir (repeat visits/next launch need no network re-fetch), with the extracted frame downscaled (e.g., to ~720px wide) to reduce memory and disk use. Slow cover loading is reported as a bug; the user accepts that a never-loaded video's first cover still needs the video header data but expects every subsequent view to be instant. Confidence: 0.65
- Sensitive credential/config files that embed API keys (e.g., `google-services.json` with the Firebase API key, `supabase.properties` with the service-role key) must be kept out of git and never uploaded to a repository — especially a public one — to protect the keys from exposure. The user cares about secret hygiene and will proactively ask to withdraw such a file after pushing it (even asking to remove it from history, checking whether the repo is public to gauge risk). When a secret has already been committed and pushed, the expected response assesses exposure (which commits contain it, is the repo public, is other high-value data like service-role keys also present), and the durable preference is to prevent recurrence (gitignore/untrack) while acknowledging that once pushed to a public history the key must be treated as compromised/rotated regardless of removal. Confidence: 0.7
- When asking about an existing feature or code area of their own project, expects a comprehensive, structured walkthrough rather than a terse summary: enumerate every involved file with its path and role grouped by layer (pages/composables, ViewModel/state, data/repository/network, utils/reusable components, navigation wiring, resources, SQL scripts), also spell out the "knowledge points"/technologies actually used (Compose + Material3, ViewModel + StateFlow, coroutines, the networking library in use, persistence, regex validation, Android basics), and trace the end-to-end data flow for a representative action (e.g., one login) — plus flag noteworthy observations (misnamed files, weak security, hardcoded keys). Confidence: 0.55

