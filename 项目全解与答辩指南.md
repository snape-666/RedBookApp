# RedBook 仿小红书 App —— 项目全解与答辩指南

> 面向毕业设计/课程验收答辩的完整技术讲解文档。
> 配合代码逐行阅读,结合末尾的「答辩必背 Q&A」食用效果最佳。

---

## 目录

1. 项目一句话介绍与亮点
2. 技术栈总览
3. 系统架构与分层
4. 导航系统(手写屏幕栈)
5. 后端设计与数据库(Supabase)
6. 与后端通信的两种方式(PostgREST + Realtime)
7. 登录注册与 xhs_id 体系
8. 首页信息流(关注 / 发现)
9. 帖子详情与评论
10. 发布笔记与草稿箱
11. 视频模块(Media3)
12. 点赞 / 收藏 / 关注 / 粉丝 / 备注
13. 个人主页 / 他人主页 / 资料编辑 / 资料卡
14. 搜索
15. 消息中心与通知系统(前台服务 + Realtime)
16. 私信聊天
17. 隐私设置与帖子可见性
18. IP 归属地
19. AI 评论小助手
20. 主题与共享组件
21. 本地存储方案
22. 核心技术知识点清单(面试/答辩用)
23. 答辩必背 Q&A
24. 附:代码文件索引

---

## 1. 项目一句话介绍与亮点

**一句话介绍**:本项目是一个使用 Kotlin + Jetpack Compose 开发的仿小红书风格的 Android 社交笔记 App,前端覆盖图文/视频发布、评论互动、关注、私信、实时通知等完整社交链路,后端使用 Supabase(PostgreSQL + PostgREST + Realtime + 对象存储)实现数据存储、实时推送与文件上传。

**答辩亮点(先背下来)**:
- 完整闭环的社交功能:发布 → 浏览 → 互动(赞/藏/评/关注) → 通知(实时推送) → 私信。
- **不依赖官方 Supabase SDK**:手写 Volley 封装 PostgREST 协议、手写 OkHttp WebSocket 实现 Realtime(Phoenix 协议),体现对 HTTP/REST/WebSocket 底层原理的理解。
- 单 Activity + 手写导航栈 + 纯 Compose UI,Material 3 主题,小红书品牌红色系。
- 实时系统设计完整:前台服务常驻收 WebSocket → 本地通知 + 应用内角标 + 点击深链路由。
- 数据设计有讲究:双主键(自增 id + 业务 TEXT id)、冗余快照、版本号双存(本地 SharedPreferences + 云端列,version 大者胜)。

---

## 2. 技术栈总览

| 层 | 技术 | 说明 |
|---|---|---|
| 语言 | Kotlin | 全 Kotlin |
| UI | Jetpack Compose + Material 3 | BOM 2026.06.01,自定义「小红书红」主题 |
| 架构 | 单 Activity + MVVM | 手写 Screen 栈;ViewModel + StateFlow |
| 图片加载 | Coil (coil-compose 2.7.0) | AsyncImage |
| 网络 REST | Volley 1.2.1 | 直连 Supabase PostgREST HTTP API |
| 实时通信 | OkHttp 3.12.13 WebSocket | 手写 Supabase Realtime(Phoenix Channels) |
| 视频 | Media3 / ExoPlayer 1.8.0 | 播放 + 512MB 磁盘缓存 |
| 协程 | kotlinx-coroutines 1.7.3 | 网络转协程、并发聚合 |
| 后端 | Supabase | PostgreSQL + PostgREST + Realtime + Storage |
| 本地存储 | SharedPreferences | 会话、搜索历史、通知/隐私设置缓存 |
| AI | DeepSeek(文本)+ 豆包/火山方舟(多模态) | 评论区 AI 小助手,自动降级 |

**重要差异点(答辩必讲)**:项目**没有使用** Supabase 官方 Kotlin SDK、没有 Jetpack Navigation、没有 Hilt、没有 Room/DataStore。一切从简但讲清原理,这是本项目最大的技术特色,也是最容易被追问的地方(见 Q&A)。

配置要点:
- `applicationId = com.example.redbook`,compileSdk/targetSdk = 37,minSdk = 24。
- `buildConfigField` 从根目录 `supabase.properties` 注入 6 个密钥:`SUPABASE_SERVICE_ROLE`、`RESEND_API_KEY`、`DEEPSEEK_API_KEY`、`VISION_API_KEY`、`VISION_BASE_URL`、`VISION_MODEL`。
- Manifest 权限:网络、精/粗定位、通知(POST_NOTIFICATIONS)、前台服务(FOREGROUND_SERVICE + DATA_SYNC)。
- 组件:仅 1 个 Activity(`MainActivity`)+ 1 个前台服务(`NotificationService`)。

---

## 3. 系统架构与分层

```
┌────────────────────────────────────────────────┐
│ MainActivity (单 Activity 入口)                 │
│  ├─ RedBookTheme                                │
│  └─ AppScreen                                   │
│      ├─ 手写导航栈 screenStack: List<Screen>     │
│      ├─ 全局状态:未读数、selectedPostId、refreshKey│
│      └─ 全局任务:恢复登录/通知服务/Realtime/定位  │
├────────────────────────────────────────────────┤
│ UI 层(按 feature 分包)                          │
│  HomeScreen/DetailScreen/ProfileScreen/...      │
│  共享组件 ui/component/(PostCard、Bar、评论等)   │
├────────────────────────────────────────────────┤
│ 状态层 MVVM                                     │
│  XxxViewModel extends AndroidViewModel          │
│  MutableStateFlow → collectAsStateWithLifecycle │
│  手写 ViewModelProvider.Factory + key 隔离      │
├────────────────────────────────────────────────┤
│ 数据层 data/                                    │
│  model/  (Note、PostDetail、Comment…)            │
│  repository/ (SupabaseAuthRepository、          │
│   RealtimeRepository、HomeRepository、           │
│   IpLocationProvider、AiAssistant)               │
│  local/   SessionPrefs(SharedPreferences)       │
├────────────────────────────────────────────────┤
│ 通知层 notification/                            │
│  NotificationService(前台服务+WebSocket)         │
│  NotifHelper/NotifPrefs/NotifClickRouter        │
└────────────────────────────────────────────────┘
           ↓ 网络(无官方 SDK)
  Supabase: PostgREST(/rest/v1/*) + Auth(/auth/v1/*)
            + Realtime(/realtime/v1/websocket) + Storage(/storage/v1/*)
```

**MVVM 数据流范例**(以首页为例):
1. `HomeScreen` 组合函数里 `viewModel(…)` 拿 `HomeViewModel`。
2. ViewModel 用 `viewModelScope.launch` + `Dispatchers.IO` 调 `HomeRepository`。
3. Repository 用 Volley 发请求,`suspendCancellableCoroutine` 把回调转成挂起函数。
4. 结果写入 `MutableStateFlow<HomeUiState>`(sealed class:Loading/Success/Error)。
5. Screen 用 `collectAsStateWithLifecycle()` 订阅,重组 UI。

---

## 4. 导航系统(手写屏幕栈)

文件:`MainActivity.kt`

- 用 `sealed class Screen`(27 个 object,如 `Login/Home/Detail/Profile/…`)表示所有页面。
- 导航状态就是 `screenStack: List<Screen>`。
  - `navigateTo(screen) = screenStack + screen`(入栈)
  - `goBack() = dropLast(1)`(出栈)
- `when(currentScreen)` 分发到对应 Composable。
- 跨页参数不塞进导航对象,而是放 AppScreen 层 `remember { mutableStateOf }`:如 `selectedPostId`、`chatPeerUid`,配合一堆 `refreshKey`(返回上一页后强制刷新,如 `detailRefreshKey++`)。
- 底部导航:`ui/component/Bar.kt` 里的 `BottomBar`,四个主页面(首页/视频/消息/我的)各自内嵌自己的 BottomBar;中央 `+` FAB 进入发布页。

**知识点**:单 Activity + 纯 Compose 的状态驱动导航;没有用 Navigation-Compose,是为了少引入一个框架,直接用 List 模拟返回栈,思路与 Fragment 返回栈一致(压栈/弹栈)。

---

## 5. 后端设计与数据库(Supabase)

> 建表 SQL 全部在项目根目录 `supabase_*.sql`(共 11 个),需在 Supabase SQL Editor 手动执行。它们是增量脚本:基础表(users/posts/comments)在控制台手建,脚本负责补列与新表。

### 5.1 表结构与职责

| SQL 文件 | 功能 | 涉及表/列 |
|---|---|---|
| supabase_xhsid.sql | xhs_id 体系 + 用户资料扩展 | users 补 xhs_id/nickname/avatar_url/background_url/gender/birthday;likes/favorites/comments/posts/drafts 补快照列 |
| supabase_publish.sql | 草稿箱 + 帖子图片 | 建 drafts;posts 补 image_url |
| supabase_likes_favorites.sql | 点赞 / 收藏 | 建 likes、favorites |
| supabase_follows.sql | 关注 + 备注 | 建 follows、remarks(UNIQUE viewer_uid+target_uid)|
| supabase_video.sql | 视频笔记/评论(遗留) | 建 video_notes、video_comments |
| supabase_browse.sql | 浏览记录 | 建 browsing_history |
| supabase_realtime.sql | 实时通知 + 私信 | 建 notifications/conversations/messages,并加入 realtime 发布 |
| supabase_notification_settings.sql | 通知偏好 | users 补 notif_* 5 开关 + notif_version |
| supabase_privacy.sql | 主页隐私开关 | users 补 privacy_posts/comments/favorites/likes + privacy_version |
| supabase_post_visibility.sql | 帖子可见性 | posts 补 visibility('public'/'private') |
| supabase_ip_location.sql | IP 归属地 | users 补 ip_location |

### 5.2 核心表速查

- **users**(应用用户表,与 GoTrue auth 配套):uid、account、email、nickname、xhs_id、avatar_url、background_url、gender、birthday、ip_location、reset_code/reset_code_expiry、privacy_*(4+version)、notif_*(5+version)。
- **posts**(图文/视频笔记统一表):post_id、title、content、author_uid/name/xhs_id/avatar、image_url(多图逗号分隔;视频带 `video:` 前缀)、visibility、like_count/favorite_count/comment_count/view_count、ip_location、created_at。
- **comments**(评论/回复一张表):comment_id、post_id、parent_id(**空=一级评论,非空=回复**)、content、author_uid/name/xhs_id、post_title、image_url(图片评论)、like_count、ip_location、created_at。
- **drafts**(草稿):draft_id、title、content、author_*、image_url、created_at/updated_at。
- **likes / favorites**(点赞/收藏,结构平行):like_id/fav_id UNIQUE、user_uid、user_xhs_id、post_id、created_at。
- **follows**:follower_uid、followed_uid、created_at(无唯一约束,应用层保证不重复)。
- **remarks**:viewer_uid、target_uid、remark(我给对方的备注名),UNIQUE(viewer,target)。
- **browsing_history**:user_uid、post_id、created_at(先删后插保持"只看最新一条")。
- **notifications**:notif_id、recipient_uid(接收者)、actor_uid/name/avatar(触发者快照)、type(like/favorite/comment/reply/follow)、post_id/post_title、comment_id/comment_content、created_at、is_read。
- **conversations**(私信会话):conversation_id = `c_{小uid}_{大uid}`(字典序),user_a_uid/user_b_uid、last_message/last_time,UNIQUE(a,b)。
- **messages**:message_id、conversation_id、sender_uid、receiver_uid、content、media_url、created_at、is_read。
- **video_notes / video_comments**:遗留表(早期视频方案),新代码视频已统一存 posts(image_url=`video:https://…`),这两张表仅保留兼容查询。
- **存储桶**:`post-images`(图片、视频都传这里,公开读)。

### 5.3 数据库设计五大特点(答辩重点)

1. **双主键**:每张表 `id BIGINT GENERATED ALWAYS AS IDENTITY`(物理自增主键)+ `xxx_id TEXT UNIQUE`(业务主键,客户端生成)。
2. **无物理外键**:全库不用 REFERENCES,表关系靠 TEXT id 在应用层拼装(查询时用 `in/eq/or` 拼条件),配合**快照冗余**(通知表存 actor_name、消息会话存 last_message…)避免 JOIN,并保证历史记录不受改名影响。
3. **时间用 BIGINT 毫秒时间戳**(epoch ms),不是 TIMESTAMPTZ,由客户端生成——与"业务 id 客户端生成"配套。
4. **权限模型简单化**:所有表 `DISABLE ROW LEVEL SECURITY` + `GRANT ALL TO anon, authenticated`;客户端只用 anonKey,数据过滤(如 private 帖只作者可见)在**应用层**做。
5. **设置类字段全部挂在 users 表上** + `xxx_version` 版本号,实现"本地缓存 + 云端"双存合并(version 大者胜)。

---

## 6. 与后端通信的两种方式

### 6.1 PostgREST(Volley 手写封装)

核心文件:`data/repository/SupabaseAuthRepository.kt`(1400 行,项目最大仓库类)
配置:`SupabaseConfig` object —— url 与 anonKey 硬编码,serviceRole/resendKey 来自 BuildConfig。

统一 8 个 REST 基元(全部用 `suspendCancellableCoroutine` 把 Volley 回调转为协程挂起):
- `queryRest(table, query)` → `GET {url}/rest/v1/{table}?{query}`
- `supabasePostBody(path, body)` / `supabasePost` → `POST`
- `patchRest / patchUserRow / patchDraftRow / patchCommentRow` → `PATCH`
- `supabaseDelete(path)` → `DELETE`(带 `Prefer: return=minimal`)
- `upsertRest(path, body)` → `POST` + `Prefer: resolution=merge-duplicates`(备注 upsert 用)
- `supabaseAdminPut`(serviceRole Bearer,仅改密码)

**PostgREST 查询语法范例**(需要会背几个):
- 过滤:`?user_uid=eq.xxx`
- 组合过滤:`?or=(user_a_uid.eq.x,user_b_uid.eq.x)`
- 模糊搜索:`?or=(nickname.ilike.*关键词*,xhs_id.ilike.*关键词*)`
- 排序+条数:`?order=created_at.desc&limit=50`
- 批量 in:`?post_id=in.(p1,p2,p3)`
- 修改返回:`Prefer: return=minimal`

### 6.2 Realtime(OkHttp 手写 WebSocket)

核心文件:`data/repository/RealtimeRepository.kt`

- URL:`wss://…/realtime/v1/websocket?apikey=…&vsn=1.0.0`
- 连接建立后发送 **Phoenix Channels 协议**的 join 帧:
  ```json
  {"topic":"realtime:notifications","event":"phx_join",
   "payload":{"config":{"postgres_changes":[{"event":"INSERT","schema":"public",
     "table":"notifications","filter":"recipient_uid=eq.当前uid"}]}},"ref":递增序号}
  ```
- 服务端推送 `event == "postgres_changes"` 帧,按 `payload.table` 分发给 `onNotification/onMessage`。
- 订阅两张表:`notifications`(过滤 recipient_uid=自己)、`messages`(过滤 receiver_uid=自己)。
- 自己产生的通知(actor_uid==自己)丢弃,避免自刷。
- 断线 3 秒后自动重连;同一 uid 复用连接,换账号重建。
- 供三层使用:AppScreen(角标)、各页面(实时刷新当前页)、NotificationService(后台系统通知)。

### 6.3 文件上传(Storage)

`uploadImage(uri, context)`:`POST {url}/storage/v1/object/post-images/{文件名}` 裸字节上传(Bearer anonKey),返回公开 URL。图片 URL 直接存,视频返回 `video:` + URL 前缀。头像、背景图、帖子图、评论图、私信附件全部复用这个函数。

---

## 7. 登录注册与 xhs_id 体系

文件:UI `ui/auth/`(LoginScreen/RegisterScreeen/AuthViewModel);数据 `SupabaseAuthRepository` 认证部分;本地 `data/local/SessionPrefs.kt`。

流程:
1. **注册**:查 `users.account` 查重 → 客户端生成 9 位纯数字 **xhs_id**(小红书号,带查重循环)→ `POST /auth/v1/signup`,user_metadata 里带 account/nickname/xhs_id → 回写 users 表。
2. **登录**:输入含 @ 按邮箱、否则按账号先查 email → `POST /auth/v1/token?grant_type=password`(GoTrue)→ 解析返回的 user + user_metadata + 查 users 表补齐资料 → 得到 `UserData(uid,email,account,nickname,xhsId,…)`。
3. **会话持久化**:**不保存 JWT**!只把 UserData 写 SharedPreferences(`SessionPrefs`,pref 名 `login_session`)。冷启动 `SessionPrefs.load` 有值就直达 Home。
4. **忘记密码**:`requestResetCode` 写 `users.reset_code`(6 位码、5 分钟过期),可选走 Resend 发邮件 → `verifyCodeAndReset` 用 **serviceRole** 调 GoTrue Admin API 改密。

知识点:
- xhs_id 是"账号身份证",写帖子/评论/草稿时**冗余**存 `author_xhs_id`;拉某人内容时 xhs_id 优先、uid 兜底(因为历史数据 xhs_id 可能没回填)。
- `SupabaseAuthRepository` 伴生对象维护 `currentUserName/currentUserAvatar`(@Volatile),供写通知 actor 名/头像使用。
- 校验规则:`ui/utils/Validator.kt` —— 账号 8~18 位字母数字;密码 8~18 位必须含字母+特殊字符;邮箱正则 + 中文错误提示。

---

## 8. 首页信息流(关注 / 发现)

文件:`ui/home/HomeScreen.kt`、`HomeViewModel.kt` + `HomeViewModelFactory.kt`;数据 `data/repository/HomeRepository.kt`。

- **双 Tab**:顶部 `HomeTopBar`「关注 / 发现」。发现=全量 posts(过滤 private);关注=先 `getFollowingUids` 拿我关注的人,再过滤出这些作者的帖子。
- **双列瀑布流**:`LazyVerticalStaggeredGrid(StaggeredGridCells.Fixed(2))`,每张卡 `PostCard`(3:4 封面 + 标题 + 作者 + 点赞数)。
- 下拉刷新 `PullToRefreshBox`。
- 状态:`HomeUiState = Loading | Success(notes) | Error`,`StateFlow` 承载。
- 点赞:**乐观更新** —— 先改本地列表状态(红心+计数),再后台 `recordLike + updatePostLike` 落库。
- 作者名统一做**备注替换**:`applyRemarks` 把"我给作者的备注名"替换显示名。
- 空表兜底:`seedMockPosts()` 造 20 条 mock 帖子,保证演示不空屏。
- 图片用 Coil `AsyncImage`,视频卡片用 `VideoThumb`(取封面帧,见第 11 节)。
- 无分页,一次性拉全表倒序(简化方案,答辩可提"改进方向:游标分页")。

`Note` 模型:id、封面图(本地 res 或 URL)、标题、作者头像/名字、点赞数、是否已赞、authorUid。

---

## 9. 帖子详情与评论

文件:`ui/PostDetail/DetailScreen.kt`、`DetailViewModel.kt`、`DetailUiState.kt`;组件 `PostContent/CommentItem/ReplyItem/CommentInputArea/KeyboardInputBar/MentionHighlight/AuthorTag/PostEditPanels`。

- 详情加载(loadPost):取 post → `isPostVisibleTo` 校验(private 非作者=不可见)→ 组装 PostDetail(浏览数先+1 乐观)→ 取评论 → `getAvatarsByUids` 补头像 → 查我是否赞/藏/关注 → 备注替换 → 兜底作者 IP → 渲染。失败 fallback mock。
- **正文区**:多图用 `HorizontalPager` 横滑 + 页码点;视频为黑色占位;标题、正文、时间·IP、浏览量。
- **评论体系(重点)**:
  - comments 一张表,`parent_id` 空 = 一级评论,非空 = 回复。
  - `getComments` 拉到全部后在内存按 parent_id 拆成一级 `Comment` + 内嵌 `replies: List<Reply>`。
  - 支持**图片评论**(评论附图,image_url 列)、**二级回复**、**点赞评论**(`updateCommentLike` 只改计数)、作者可删除、作者标签 AuthorTag、每条带 IP 归属地。
  - 回复/艾特 UI:`KeyboardInputBar` 浮层键盘(imePadding),`MentionHighlight` 把 `@xxx` 蓝色高亮。
- **互动操作**:底部 `NoteCardBottomBar` 常驻:点赞/收藏/评论,`remember` 局部计数先变,再调仓库落库;收藏会 `notifyPostOwner(…,"favorite")`。
- 作者能力:长按/菜单可**编辑帖子**、**切换可见性**(public/private,`PostPermissionPanel`)、**删除帖子**;详情加载完 `incrementViewCount`。
- 详情页在 ON_RESUME 时重同步关注状态;注册 Realtime 监听实现"别人给我评论时本页不刷新也能看到角标变化"。
- **AI 小助手**入口:输入 `@小助手:问题` 触发(见第 19 节)。

---

## 10. 发布笔记与草稿箱

文件:`ui/publish/PublishScreen.kt`、`PublishViewModel.kt`;`ui/draft/DraftScreen.kt`;发布页支持三种模式:新发布 / 编辑草稿(editDraft)/ 编辑已发布帖(editPost)。

- **多图选择与拖拽排序**:`DraggableImageRow`,长按 `detectDragGesturesAfterLongPress` 拖动换位(越过半格交换,`animateItem()`),最多 11 张,右上角可删。
- **图文 vs 视频判定**:通过文件扩展名或 contentResolver 的 mime `startsWith("video")` 进入视频模式。
- **视频发布**:预览用 `VideoView` + `MediaMetadataRetriever` 取宽高适配;上传走 `uploadImage`,得 `video:{url}`,**统一写入 posts 表**;图文的 image_url 是逗号分隔的多图 URL(远端 URL 用 `,` 拼接,本地 URI 用 `;` 暂存,发布前统一处理)。
- **草稿**:存草稿时把本地 content:// 图片先 `cacheImage()` 复制到 filesDir 成 file:// 路径(可离线后继续发),远端图直接存 URL;草稿表 drafts 云端保存(`saveDraft/updateDraft/getDrafts/deleteDraft`)。
- **编辑已发布帖**:保留原 post_id 走 `updatePost`;已是远端 http 的图直接保留 URL,只上传新加的本地图;可见性用 `editPostVisibility` 回写。
- 标题 ≤20 字限制;正文区可@提及展示。
- DraftScreen:双列瀑布流展示云端草稿(缩略图、时间、删除按钮),点击回填发布页继续编辑。
- PublishViewModel 由 `object PublishViewModelBuilder.build(...)` 手工 new(全项目唯一不走 Factory 的)。

---

## 11. 视频模块(Media3)

文件:`ui/video/Media3Player.kt`、`VideoFeedScreen.kt`、`VideoDetailScreen.kt`;封面 `ui/component/VideoThumb.kt` + `VideoThumbCache.kt`。

- **播放器工厂** `Media3PlayerFactory`:ExoPlayer + `SimpleCache`(**512MB LRU 磁盘缓存**)+ `CacheDataSource` 边播边缓存 + 积极缓冲(`setBufferDurationsMs`,起播快)。
- `rememberVideoPlayer(url)`:Composable 里 remember 创建播放器、prepare、`DisposableEffect` 负责 release(离开页面释放)。
- **视频 Feed 页**:从 posts 表筛 `image_url` 带 `video:` 前缀的行 → `VerticalPager` 全屏上下滑(beyondViewportPageCount=1 预加载下一屏);黑底 `PlayerView` 竖屏 ZOOM 铺满;点按播放/暂停(叠加图标);进度条+剩余时间用 LaunchedEffect 轮询(50/100ms);onResume 自动播、ON_PAUSE 暂停。每屏带:关注按钮、评论浮层(0.75 屏高 LazyColumn)、点赞/收藏、AI 评论。
- **视频详情页**:上半视频 + 下半操作区,复用图文详情的评论/权限/编辑组件。
- **封面帧缓存** `VideoThumbCache`:`MediaMetadataRetriever.setDataSource(url)` 远程取帧 → 统一缩放 720px 宽 JPEG → 内存 LRU(20MB)+ 磁盘缓存(cacheDir/video_covers),Feed 卡片/浏览记录/通知列表渲染视频封面不卡。
- 计数/评论与图文共用 posts + comments(post_id=视频 id)。

---

## 12. 点赞 / 收藏 / 关注 / 粉丝 / 备注

### 12.1 点赞与收藏
- 两个平行表 likes / favorites;记录主键 like_id=`l_{uid}_{postId}`、fav_id=`f_{uid}_{postId}`(天然幂等,可判断"是否已赞")。
- `recordLike(userUid, postId, liked=true)` = 有则插、无则删;同时 `updatePostLike(postId, delta)` 读-改-写 posts.like_count 冗余计数。
- 交互即写 notifications:`notifyPostOwner`(跳过自己、用全局 currentUserName/Avatar 当触发者快照),type=like/favorite。
- 主页「赞过/收藏」两个 tab:先 `getLikedPostIds/getFavoritedPostIds` 拿 id 集合,再 `or=(post_id.eq…)` 一次拉回帖子,避免 N+1。

### 12.2 关注与粉丝
- `follow(followerUid, followedUid, bool)` = INSERT/DELETE follows;关注成功 `notifyFollow` 写通知。
- 查询:isFollowing(单条)/isFollowingBatch(or 批量)/isMutualFollow(互关)/getFollowingUids/getFollowerUids/getFollowingCount/getFansCount。
- 主页统计:获赞/收藏数 `getLikeCount/getFavoriteCount` = 遍历本人 posts+video_notes 的 like/favorite_count 求和。
- FollowListScreen(关注/粉丝两 mode)算 `followedByMe/followsMe/isMutual` 显示「已关注/回关/互相关注」。

### 12.3 备注名(本项目一个巧设计)
- remarks 表:`viewer_uid` 我给 `target_uid` 起的备注,UNIQUE(viewer,target),写入用 upsert(PostgREST `Prefer: resolution=merge-duplicates`)。
- **全局替换展示**:首页信息流、帖子详情作者、评论列表、关注列表、搜索、会话列表……凡是显示别人的名字处都先查我的 remarks,有备注就显示备注名。代码体现为 `applyRemarks/getRemarks(viewerUid, targetUids)` 批量化。

---

## 13. 个人主页 / 他人主页 / 资料编辑 / 资料卡

文件:`ui/profile/ProfileScreen.kt`、`ProfileViewModel.kt`;`ui/editprofile/EditProfileScreen.kt`;`ui/profile/UserCardScreen.kt`。

- **一个 ProfileScreen 两用**:自己主页 vs 他人主页,靠 `isSelf = viewerUid.isBlank() || userUid == viewerUid` 区分;ViewModel 用 Factory + `key` 隔离(`"self_profile_$uid"`/`"user_profile_$uid"`),避免多账号串实例。
- `refresh()`:**10 个协程 async 并发拉取** 帖子/草稿数/赞过/收藏/我的评论/点赞id集合/关注数/粉丝数/获赞数/收藏数 → 过滤 private → 备注替换 → 写 StateFlow。
- UI:
  - 自己:背景图、头像、昵称、小红书号、IP、性别年龄标签、关注/粉丝/获赞与收藏统计、浏览记录入口、TabRow(笔记/评论/收藏/赞过,**按隐私开关显示**)、侧滑抽屉(草稿箱/浏览记录/账号/改密/通知设置/隐私设置/退出/日夜主题)。
  - 他人:关注按钮/发私信/「资料卡」菜单、ActionSheet(设置备注名/取消关注)、RemarkDialog。
- 实时性:主页注册 RealtimeListener,收到 follow/like 等就 `viewModel.refresh()`;ON_RESUME 也刷新。
- 编辑资料(无 VM,直接用 remember repository):头像/背景 `GetContent` 选图 → uploadImage → `updateUserProfile`;性别三选;生日三滚轮(`LazyColumn` + `snapshotFlow` 定位 + `SnapFling`);保存后 `onDataChanged` 回写 AppScreen 内存状态与全局 currentUserAvatar。
- 资料卡 UserCardScreen:从云端拉 uid/昵称/性别/生日/地区/主页背景展示成卡片样式(仿小红书"更多资料")。
- 关注按钮旁统计获赞与收藏的弹窗 showLikeFavDialog。

---

## 14. 搜索(三块)

1. **搜帖子** `ui/search/SearchScreen.kt`:`SharedPreferences("search_history")` 存历史;逻辑=拉全量 posts → 客户端 `title.contains` 过滤(带备注替换);「推荐搜索词」=`getPostsByViews()`(view_count desc limit 6)清洗标题;历史记录可删除/清空。
2. **搜联系人** `MessageSearchScreen`(300ms **防抖** LaunchedEffect):我关注的人里本地模糊匹配备注/昵称。
3. **搜聊天记录** `MessageSearchViewModel`:服务端 `searchMyMessages` = messages 表 `or=(sender_uid.eq,receiver_uid.eq)` + `content=ilike.*关键词*` 倒序 100,再批量补对端昵称头像与备注;结果里关键词高亮 `highlightKeyword`,点击跳会话并定位消息。

---

## 15. 消息中心与通知系统(重点模块)

### 15.1 消息中心 `MessagesScreen`
顶栏(搜索+添加好友)→ 三个分组入口:
- **收到的赞和收藏**(红,type like/favorite)
- **新增关注**(蓝,follow)
- **新增评论**(绿,comment/reply)

各自挂 `CountBadge` 未读角标 → 点击清零并 `markNotificationsRead(uid, types)`。
下方是私信会话 LazyColumn(`ConversationsViewModel`:conversations 表 or 查询 + 每会话未读数 + 对端资料 + 备注替换)。

三个通知明细页(ReceivedReactions/ReceivedComments/Followers)共用 `NotificationsViewModel`,按 type 过滤 notifications 表倒序 100;每页都实时监听 Realtime(type 命中即 reload);点击通知行定位:`highlightActorUid` → `animateScrollToItem` + 0.5s 高亮。评论页会 `getExistingCommentIds` 判断原评论是否已删(显示「该评论已删除」)。

### 15.2 未读角标(全局)
AppScreen 维护 `unreadLikesFavs/unreadFollows/unreadComments/unreadMessages`,登录后先 `getUnreadCounts` 拉存量,再订阅 Realtime **增量 ++**;进对应页清零。

### 15.3 前台服务(后台也能收推送)
文件:`notification/NotificationService.kt`(Manifest 已注册,`foregroundServiceType="dataSync"`,START_STICKY)。

- 登录成功后 `startNotificationService()`,登出停止。
- 服务自己持有 RealtimeRepository 的 WebSocket,**App 退到后台也能收**。
- 启动时 `catchUpOffline`:按 NotifPrefs 里 `{uid}_last_notified` 水印补发离线期间的通知/私信。
- 收到事件按 `NotifPrefs.load(uid)` 的 5 个开关决定是否弹**系统通知**。
- `NotifHelper`:两条渠道 `redbook_interactions` / `redbook_messages`;通知 id = `2000 + eventId.hashCode()%1000`(同事件去重更新)。
- `NotifClickRouter`:通知 → `PendingIntent(ACTION_NOTIF_TAP)` → MainActivity `onNewIntent`/冷启动 intent → `handleNotificationTap`:like/favorite→赞藏页、comment→评论页、follow→关注页、dm→先 `getOrCreateConversation` 再进 Chat。
- RedBookApp(Application)onCreate 创建通知渠道。

---

## 16. 私信聊天

文件:`ui/messages/ChatScreen.kt`、`ConversationsViewModel.kt`。

- 会话唯一化:conversation_id = `c_{字典序小uid}_{字典序大uid}`;查不到就 `getOrCreateConversation` 新建(POST conversations)。
- 发消息:`sendMessage` POST messages,再 PATCH conversations 的 last_message/last_time(会话列表排序与摘要);支持图片附件(media_url)。
- 聊天页用**全局 Realtime listener** 收 `onMessage` 增量追加(自己发的直接追加不回显);`markConversationRead` 清未读。
- 会话列表:or 双条件查 → 逐会话补未读 + users 资料 + remarks 备注名。
- 消息时间显示 `ui/utils/formatChatTime.kt`(今天 HH:mm / 昨天 / 前天 / N天 / N周 / N月 / 完整日期)。

---

## 17. 隐私设置与帖子可见性

- **主页隐私开关**(`PrivacySettingScreen` + PrivacySettingViewModel):
  - 四行开关:展示 笔记/评论/收藏/赞过。
  - **双存**:本地 `PrivacyPrefs`(SharedPreferences,按 uid 隔离)+ 云端 users 表 privacy_* 列 + `privacy_version` 版本号;保存时 version+1,写完回读校验。
  - 加载策略:自己主页=本地先显示、云端 version ≥ 本地才覆盖(秒开+最终一致);看别人主页=只读云端,关掉的 tab 直接隐藏。
- **帖子级可见性**(post_visibility):posts.visibility = 'public'/'private'。
  - 详情页作者模式 `PostPermissionPanel` 切换;发布/编辑时回写。
  - 所有"拉帖子列表"的地方(首页/搜索/主页/浏览记录)统一过 `filterVisiblePosts`:private 只有作者本人可见。
  - 直接打开 private 帖链接:`isPostVisibleTo` 校验,非作者视为「已删除或不可见」。
- **通知设置**(NotificationSettingScreen,内联逻辑无 VM):总开关「接收消息通知」+ 分类(赞和收藏/新增关注/评论/私信);同样本地 NotifPrefs + 云端 notif_* + notif_version;总开关关闭时分类灰掉;提供跳系统通知设置 `ACTION_APP_NOTIFICATION_SETTINGS`;前台服务弹窗前按开关过滤。

---

## 18. IP 归属地

文件:`data/repository/IpLocationProvider.kt`(object)。

- 双方案**竞速**(协程 async + `withTimeoutOrNull(7s)`):
  1. **定位方案**:LocationManager lastKnown/requestSingleFix(6s 单次定位回调)→ 逆地理编码(系统 Geocoder,Locale.CHINA;失败兜底 Nominatim OSM)。
  2. **公网 IP 方案**:myip.ipip.net 与 api.vore.top 并发取最先返回的中文结果。
- `cleanProvince`:"中国湖北省武汉市"洗成"湖北",直辖市特殊处理;进程内 `@Volatile cachedProvince` 缓存。
- 写入/使用:MainActivity 定位授权后 `refreshMyIp()` → `updateUserIpLocation` 写 users.ip_location;发帖/评论/回复时 `resolveProvince ?: "未知"` 冗余进 posts/comments 的 ip_location;展示于 主页头部、详情"时间·IP"、每条评论、资料卡地区。
- 兜底链:老帖作者 posts.ip_location 为空 → 查作者 users.ip_location → 本机缓存。

---

## 19. AI 评论小助手

文件:`data/repository/AiAssistant.kt`(object),常量身份 uid=`ai_assistant`、昵称"小助手"。

- 触发:评论或回复以 `@小助手：` / `@小助手:` 开头(`parseQuestion` 抽问题)。
- 双模型**自动降级**:
  1. 首选**多模态**:豆包/火山方舟(VISION_* BuildConfig,OpenAI 兼容 `/chat/completions`),把帖子的 image_url / video_url 直接传给模型看图/看视频回答。
  2. VISION_KEY 为空或调用失败 → 降级 **DeepSeek**(api.deepseek.com/chat/completions,deepseek-chat)纯文本。
- 回复**落库**:以小助手身份 POST 一条 comments,parent_id = 提问的评论 id → 提问者/所有人能在评论区看到回答。
- UI 交互:发问后先插入"小助手正在思考中…"占位,拿到回答后替换。
- 视频 Feed、视频详情、图文详情三处复用。

---

## 20. 主题与共享组件

### 20.1 主题 `ui/theme/`
- `Theme.kt`:`RedBookTheme(darkTheme, dynamicColor=false)`,Material 3 colorScheme,**禁用动态取色**保证品牌色;Typo 自定义(bodyMedium 14sp 等)。
- `Color.kt`:亮/暗两套。亮色 primary `#FF2741`(小红书红);语义色:话题蓝、红底 #FFE0E1、点赞橙、星标黄、绿(成功)、粉(女性);配套一堆 `@Composable getXxx()`(内部按 isSystemInDarkTheme 分支)。
- 日夜切换:主页抽屉里 `isDarkTheme` 手动布尔切换(未持久化,重启回跟随系统)。

### 20.2 复用组件 `ui/component/`
- PostCard(瀑布流卡片)、PostContent(详情正文:多图 Pager+视频区)、CommentItem/ReplyItem、KeyboardInputBar(imePadding 浮层输入,带图/AI)、CommentInputArea、MentionHighlight(@高亮)、AuthorTag(作者/小助手标签)、Bar.kt(BottomBar/HomeTopBar/NoteCardBar/NoteCardBottomBar/CountBadge)、PostEditPanels(作者面板/底部弹层/编辑面板/权限面板)、AppIcon、AuthInputRow、VideoThumb/Cache。
- 资源侧:res/drawable 里 61 个 vector XML 图标(chat、favorite_fill、male/female、visibility…),与 M3Theme/ 目录(官方 Material Theme Builder 导出参考)同名对应 —— 答辩可说明图标体系:Compose 用 `AppIcon` 统一封装 drawable。

---

## 21. 本地存储方案

全项目**无 Room / DataStore**,统一 SharedPreferences + 文件缓存:

| 用途 | 文件 | prefs 名/键 |
|---|---|---|
| 登录会话 | data/local/SessionPrefs.kt | `login_session`(uid/xhs_id/nickname/…) |
| 通知设置缓存 | notification/NotifPrefs.kt | `notification_settings`(按 uid 前缀隔离 + `{uid}_last_notified` 水印)|
| 隐私设置缓存 | notification/PrivacyPrefs.kt | `privacy_settings`(uid 隔离)|
| 搜索历史 | SearchViewModel 内 | `search_history` |
| 视频缓存 | Media3Player | SimpleCache 512MB 磁盘 |
| 视频封面帧 | VideoThumbCache | cacheDir/video_covers |
| 草稿本地图 | PublishViewModel | filesDir(cacheImage) |

版本合并模式(通知/隐私设置通用):本地 = 快,云端 = 权威;`云端 version >= 本地 version` 才用云端覆盖本地,保存时本地云端都写且 version 自增。

---

## 22. 核心技术知识点清单(答辩/面试都能讲)

**Kotlin / 协程**
- `viewModelScope` + `Dispatchers.IO`;`withContext` 切线程。
- `suspendCancellableCoroutine`:**把 Volley/OkHttp/定位回调转成协程挂起函数**(讲清回调地狱 → 协程的转换原理:挂起 = 保留 continuation,回调里 resume)。
- `async/await` 并发(ProfileViewModel 10 个请求并发拉取)。
- `withTimeoutOrNull` 竞速/超时(IP 双方案)。
- `StateFlow` + `asStateFlow()`(只读暴露);`collectAsStateWithLifecycle` 订阅。
- sealed class 表达 UI 状态(Loading/Success/Error),让非法状态不可表达。

**Compose**
- 单 Activity;`setContent`;状态提升(AppScreen 统一持有导航+全局态)。
- `remember/mutableStateOf/mutableIntStateOf`;`LaunchedEffect`(一次性加载/延时轮询/防抖);`DisposableEffect`(释放播放器/注销监听);`snapshotFlow`(滚轮定位)。
- `LazyVerticalStaggeredGrid` 瀑布流;`HorizontalPager` 详情多图;`VerticalPager` 视频流。
- 手势:长按拖拽排序 `detectDragGesturesAfterLongPress`;`PullToRefreshBox` 下拉刷新;`WindowInsets.isImeVisible` + imePadding 键盘浮层。
- 乐观更新 UI 模式(先改界面后落库)。
- 生命周期:`LifecycleEventObserver`/ON_RESUME 重同步。

**Android 系统**
- 前台服务(FOREGROUND_SERVICE_DATA_SYNC)+ START_STICKY + 通知渠道(Android 8+)+ POST_NOTIFICATIONS 运行时权限(Android 13+)。
- PendingIntent 通知点击路由;Action 隐式分发(ACTION_NOTIF_TAP)。
- 运行时定位权限;Geocoder 逆地理编码;MediaMetadataRetriever 取视频帧。
- GetContent(ActivityResultContracts)选图,ContentResolver 读 URI。

**网络协议(重点,体现深度)**
- REST 语义:GET/POST/PATCH/DELETE + 查询字符串过滤(eq/ilike/or/order/limit/in)。
- PostgREST 约定:`Prefer: return=minimal`、`resolution=merge-duplicates`(upsert)。
- WebSocket 长连接 + Phoenix Channels:topic/event/phx_join/postgres_changes 帧结构。
- 断线重连(3s)、心跳 ping、引用计数 ref。
- JWT/anonKey 概念、service_role 只用于 Admin 改密。

**数据库**
- PostgreSQL:IDENTITY 自增、UNIQUE、partial 思想;RLS 概念(本项目关闭,改为应用层过滤)。
- 设计模式:双主键、业务 id 客户端生成、快照冗余、BIGINT 毫秒时间、version 乐观合并。

**第三方库**:Coil 三级缓存与 AsyncImage;ExoPlayer(Media3)SimpleCache/CacheDataSource;Volley 请求队列与重试。

---

## 23. 答辩必背 Q&A

**Q1 你这个项目主要功能是什么?**
仿小红书社交笔记 App:账号体系(邮箱+密码+小红书号)、图文/视频发布、双 Tab 信息流瀑布流、帖子详情与多级评论(图片评论、AI 回复)、点赞/收藏/关注/粉丝、备注名、搜索(帖/人/聊天记录)、私信聊天、浏览记录、草稿箱、消息通知中心(实时角标+系统通知)、隐私/通知/可见性设置、IP 归属地展示、AI 评论助手。

**Q2 后端用的什么?为什么不用自建服务器?**
Supabase —— 开源的 Firebase 替代,底层是 PostgreSQL。它把数据库直接暴露成 REST 接口(PostgREST),还自带 Auth、Realtime、对象存储,省去自己写后端接口和部署,开发效率高,适合课程设计;SQL 脚本都在项目里可复现。

**Q3 客户端怎么连数据库的?用了官方 SDK 吗?**
没用官方 Supabase SDK。我是直接用 Volley 手写 HTTP 请求去调 PostgREST 接口,URL 形如 `https://项目.supabase.co/rest/v1/posts?order=created_at.desc`;查/增/改/删对应 GET/POST/PATCH/DELETE。这样做对协议理解更深:能讲清楚 REST、查询串过滤、请求头、状态码。

**Q4 那实时通知是怎么实现的?**
Supabase Realtime 本质是 WebSocket 长连接。我用 OkHttp 手动建立 `wss://…/realtime/v1/websocket` 连接,按 Phoenix Channels 协议发 `phx_join` 帧订阅数据库表的 INSERT 事件(postgres_changes),过滤条件是我自己的 uid。服务端一有新通知/新私信就推过来,客户端解析 payload 更新角标;App 退后台时由前台服务持有这条连接,收到后弹系统通知。断线还会 3 秒自动重连。

**Q5 服务端数据库怎么设计的?**
所有表:自增 id 作物理主键 + 业务 TEXT id(客户端生成,UUID 风格)唯一。没有物理外键,表与表靠 uid/id 关联 + 冗余快照(比如通知表直接存触发者昵称头像),好处是查列表不用 JOIN、用户改名不影响历史。时间统一存毫秒 BIGINT。像"赞过""私信会话"这种表都用约定 id(`l_{uid}_{post}`、`c_{小uid}_{大uid}`)天然保证唯一不重复。

**Q6 你点赞功能怎么保证不重复、怎么取消?**
like_id = `l_{用户uid}_{帖子id}`。点赞=INSERT,再查是否有我的 like_id 记录判断已赞;取消=DELETE。同时维护 posts 表 like_count 计数(先读后写 +1/-1)。

**Q7 评论和回复怎么区分的?**
同一张 comments 表,parent_id 为空就是一级评论,非空就是回复某条评论。列表接口一次拉全,客户端按 parent_id 分组嵌套展示。

**Q8 隐私设置/通知设置为什么要在本地和云端各存一份?还带版本号?**
本地 SharedPreferences 是为了秒开(不闪空),云端 users 表列是权威。带 version 是为了解决"多端/多页面同时改"的冲突:谁 version 大听谁的,保存时版本号自增。这是一种简易的乐观并发控制。

**Q9 你的视频方案?**
视频文件上传到 Supabase Storage,posts 表 image_url 存 `video:` + URL 前缀来区分图文和视频。播放用 Media3/ExoPlayer,配 512MB 磁盘 LRU 缓存,Feed 用 VerticalPager 上下滑。封面不下载整视频,而是 MediaMetadataRetriever 远程抽帧,缓存成 720px JPEG。

**Q10 为什么要把 RLS 关掉?安全吗?**
课程设计为了聚焦业务逻辑,统一关闭行级安全、由应用层做数据过滤(比如私密帖只有作者可见是客户端过滤的)。我清楚生产环境应该开启 RLS + 用用户 JWT 访问,anonKey 只暴露必要权限——这也是我后续的改进方向。

**Q11 service_role key 放客户端不安全吧?**
对,严格说 service_role 绝不能进客户端。本项目它只用于"忘记密码"调 GoTrue Admin 接口改密码这一个场景。改进方案是把它挪到 Supabase Edge Function 或自建服务端。同样属于已知的取舍点,已作为答辩中的"不足与改进"。

**Q12 数据模型的代码为什么都是普通 data class,没有 @Serializable?**
因为没引 kotlinx-serialization,JSON 解析用 Android 自带 org.json(JSONObject/JSONArray)处理。data class 是 View 层的渲染模型,由仓库层把 JSON 组装成它们。

**Q13 网络请求怎么和 Compose 配合的?会卡 UI 吗?**
不会。请求全部在 IO 线程,通过 suspendCancellableCoroutine 把 Volley 异步回调转成挂起函数,在 viewModelScope 里调用;结果用 StateFlow 发出来,UI 通过 collectAsStateWithLifecycle 收集。UI 线程只重组,不做网络。

**Q14 如果一个页面要拉很多数据怎么办?**
ProfileViewModel 主页要拉帖子、草稿数、赞过、收藏、评论、统计……用协程 async 并发 10 个请求再 await,而不是串行,耗时接近最慢的一个。

**Q15 下拉刷新、加载更多做了吗?**
做了下拉刷新(PullToRefreshBox,重新拉全量)。分页/无限滚动没做,是简化点,答辩可说:数据量上来后应改成游标分页 + LazyGrid 到底加载。

**Q16 你最近解决的最难的一个问题是什么?(答辩高频)**
备选真实素材:
1) 手写 Supabase Realtime:Phoenix 协议帧结构、postgres_changes 过滤、断线重连、同 uid 复用连接。
2) IP 归属地双方案竞速 + 长省名清洗(把"中国湖北省武汉市"规整成"湖北")。
3) 通知链路闭环:前台服务 WebSocket → 本地通知 → PendingIntent → MainActivity 路由到具体页并高亮。
4) 视频封面:远程视频抽帧 + 内存/磁盘两级缓存,避免 Feed 卡顿。

**Q17 这个项目如果继续做,你打算加什么?**
开启 RLS+JWT;真实分页;Room 缓存离线数据;Edge Function 放 service role;端到端测试;图片压缩与多端同步;帖子编辑历史;语音/位置标签发布;关注推荐。

---

## 24. 附:代码文件索引(答辩指路用)

```
com.example.redbook
├─ MainActivity.kt            入口 + AppScreen + Screen 密封类 + 导航 + 全局未读/路由
├─ RedBookApp.kt              Application,建通知渠道
├─ data/
│  ├─ local/SessionPrefs.kt           登录会话(SharedPreferences)
│  ├─ model/                          Note/PostDetail/Comment/Reply/UserComment/
│  │                                  Draft/PostToEdit/PrivacySettings/NotificationSettings
│  └─ repository/
│     ├─ SupabaseAuthRepository.kt    ★ 核心:全部 PostgREST 封装(认证/帖子/评论/赞藏/关注/资料)
│     ├─ RealtimeRepository.kt        ★ 通知+私信 REST + OkHttp WebSocket 实时
│     ├─ HomeRepository.kt            首页聚合(发现/关注/备注替换/mock)
│     ├─ IpLocationProvider.kt        IP 归属地双方案
│     └─ AiAssistant.kt               AI 小助手(火山方舟→DeepSeek 降级)
├─ notification/
│  ├─ NotificationService.kt  前台服务,持 WebSocket,弹系统通知,离线补发
│  ├─ NotifHelper.kt          通知渠道与构建
│  ├─ NotifPrefs.kt / PrivacyPrefs.kt  本地缓存(uid 隔离)
│  └─ NotifClickRouter.kt     点击路由
└─ ui/
   ├─ auth/         登录/注册/找回密码
   ├─ home/         首页(关注/发现 + 瀑布流)
   ├─ PostDetail/   帖子详情 + 评论 + 互动 + 作者编辑
   ├─ publish/  draft/   发布(拖拽多图/视频/草稿) + 草稿箱
   ├─ video/        Media3Player / VideoFeedScreen / VideoDetailScreen
   ├─ profile/  editprofile/   自己+他人主页 / 关注粉丝列表 / 隐私设置 / 资料卡 / 编辑资料
   ├─ search/       帖子搜索
   ├─ messages/     消息中心 / 通知明细 / 聊天 / 加好友 / 消息搜索
   ├─ notificationsetting/  通知设置
   ├─ component/    共享组件(卡片/评论/顶栏/弹层/视频封面…)
   ├─ theme/  utils/  小红书红主题 / 校验与格式化工具
```

**答辩演示动线建议**:注册新号 → 发一篇图文、发一篇视频 → 首页刷新看到 → 进详情自己点赞收藏评论(带图片)、@小助手 提问 → 回自己主页看统计与隐私开关效果 → 开小号关注/评论/私信 → 观察原号角标与系统通知 → 点通知跳到对应页 → 展示设置双存与搜索、备注名。

祝答辩顺利。把 Q1–Q16 背熟,再照着第 22 节的知识点各讲 30 秒,基本能应对所有提问。
